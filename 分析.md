# OLED 排线/引脚排查记录

> 目的：集中保存这块未知 OLED/FPC（16 个机械触点、当前确认 15 条有效可见线路）的接线、测量、推断和下一步操作。
> 后续任何对话开始排查前先阅读本文；新结果只追加，不覆盖旧记录。
>
> 截止日期：2026-09-24（Asia/Shanghai）
> 当前阶段：已完成受限静态/开漏负载、0x3C 有效 NACK、断电微安级配对和 WMV 显微视频复核；协议、控制器和功能针序仍未确认。公开图纸比对已排除 Adafruit 5228、GoldenMorning E109XI 精确型号和 ZJY 15P/0.65 mm 近邻件，下一步只做机械尺寸/同源料号闭环；三片样品全部断电、P1-P16 悬空，暂停刷固件和主动协议测试。

## 1. 来源与可复核性

本记录合并了以下材料：

1. 外部分享对话：<https://chatgpt.com/s/cx_6aaf354e5f68819183f98ed3f07db4a9>。2026-09-20 通过 Codex In-app Browser 直接读取，页面标题为“确认项目对话可见性 (3)”。关键读数已抄录到本文，不能只依赖分享页的后续可变内容。
2. 本地 Codex 任务：`thread://01a0b485-34a3-70e3-80dc-d5b0d35f10ee`，工作目录为 `/Users/liyao/Documents/AI/Screen`。最新可读结果包含锁定固件的 `PINS` 输出和 P11 接线纠正后的静态测量。
3. 当前仓库：`/Users/liyao/Documents/github/OLED/ESP32C3Demo`，主要参考 `README.md`、`platformio.ini`、`src/main.cpp`、`src/open_drain_test.cpp`、`src/address_ack_test.cpp`。

说明：本地任务最后有一轮因模型不受当前账户支持而失败的记录；这是工具/会话错误，不是 OLED 或 ESP32 的硬件故障。仓库当前 `platformio.ini` 仍为 `board = airm2m_core_esp32c3`，没有实际切换为 `esp32-c3-devkitm-1`。

## 2. 当前结论（先看这里）

> **重要覆盖说明（2026-09-21）**：本节表格是在微安级成对方向性测试和公开 15 针 SPI 候选出现之前的历史快照，仅用于追溯，不可直接作为接线表。后文已显示 `P6/P9` 与 `P13/P9/P10` 存在竞争解释；P6、P13 的确切电源功能以及 P9 的具体功能仍未确认。当前操作以文末最新记录为准：全部引脚悬空，暂停正常供电、协议和主动 GPIO。

### 2.1 引脚候选表

| OLED 脚 | 当前工作判断 | 证据等级 | 当前处理 |
|---:|---|---|---|
| P1 | 未知模拟/面板节点；与 P15 曾测得约 11.7 ohm | 未确认 | 悬空，禁止供电/GPIO |
| P6 | 外部 VDD 候选 | 较强工作假设，未获芯片资料确认 | 仅可经受控串阻供电 |
| P7 | 内部 VLH/I/O 参考或钳位节点候选，不是外部 VDD | 较强反证支持 | 悬空，禁止外部供电 |
| P8 | D2 / SDAOUT 候选 | 数字功能未确认；0x3C ACK 未在此脚出现 | 只作输入，勿短接 P9 |
| P9 | D1 / SDAIN 候选 | 开漏拉低/释放通过，协议功能未确认 | 仅允许受限开漏拉低 |
| P10 | D0 / SCL 候选 | 开漏拉低/释放通过；曾出现一次上升失败 | 仅允许受限开漏拉低，先查上升沿 |
| P11 | D/C# / SA0 候选 | 接地偏置和读回 LOW 已验证；功能未确认 | 旧 0x3C 测试保持 LOW；不要把无效 0x3D 结果当结论 |
| P12 | RES# 候选 | 动态拉低后可恢复，证据较强但仍非芯片级证明 | 只允许单次、受限、可验证的低态 |
| P13 | VSS/VLL 参考地候选 | FPC 大铜面、全部测量共同参考、行为一致 | 实验电源负极和 ESP32 GND 共地 |
| P14 | 未知模拟/面板节点 | 未确认 | 悬空，禁止供电/GPIO |
| P15 | 未知模拟/面板节点；与 P1 约 11.7 ohm | 未确认 | 悬空，禁止供电/GPIO |
| P16 | FPC 触点，是否为有效电气节点未确认 | 高清照片显示 16 个金手指及 `16` 标记；早期 NC 判断已降级 | 不接线，先核对方向和无电走线 |

“候选”不等于“已确认”。尤其是 P8-P12 的名字来自 SSD1312/SSD1306 类拓扑假设；在得到可信协议响应前，不应把它们写成确定引脚定义。

### 2.2 ESP32-C3 候选映射

开发板方向约定为“天线朝上、元件面朝前”。物理排针编号不是 GPIO 编号：

| OLED | GPIO | 物理排针 | 候选 |
|---:|---:|---:|---|
| P8 | GPIO0 | 左侧 02 | D2 / SDAOUT |
| P9 | GPIO1 | 左侧 03 | D1 / SDAIN |
| P10 | GPIO3 | 右侧 20 | D0 / SCL |
| P11 | GPIO4 | 右侧 28 | D/C# / SA0 |
| P12 | GPIO5 | 右侧 27 | RES# |

锁定版 `PINS` 实测输出为 `HIGH, HIGH, HIGH, LOW, HIGH`（P8/P9/P10/P11/P12），并明确关闭内部上下拉。这个结果只证明当前外部电阻网络和 GPIO 连通，不证明功能名称。

### 2.3 当前安全边界

- 所有改线、换电阻、换表笔前，OLED 实验电源和 ESP32 USB 都必须断开，并等待 P6-P13 放电到接近 0 V。
- P6 只能接受受控、限流的实验电源；P7、P1、P14、P15 不得外部供电，也不得接 GPIO。
- 五个 51k 和五个 10k（若使用）必须是相互独立的电阻；上拉只能回到 OLED P6，不能回到 ESP32 `3V3`；P8 与 P9 不得直接短接。
- 默认 PlatformIO 环境是只读锁定版，只接受 `PINS`、`STATUS`、`HELP`。主动测试必须显式选择环境，不能把普通 `Upload` 当成主动测试固件。
- 暂不运行普通 I2C 扫描、U8g2 示例、初始化/清屏或重复 ACK。未知供电域、接口模式、分离 SDA 拓扑和 51k 慢上升沿都会制造歧义。
- 出现 CC、持续掉压、跳变、发热、异味、亮点、`FAULT`、`UNVERIFIED` 或释放未验证时，立即关闭 OLED 电源；不要靠重复命令“试试”。

## 3. 按时间整理的排查过程

### 3.1 FPC 和初始拓扑判断

- 早期低清照片中的 FPC 标记曾读作 `30033 A020001`；高清照片更清楚地显示为 `30033` 和 `A0201001`，仍不能当作公开型号。照片方向和 P1/P16 完整编号需单独核对。
- 高清照片显示 16 个金手指、`16` 标记以及从接触端进入 COF 的走线。早期“P16 铜舌终止、疑似 NC”的判断只保留为历史推断，不能再作为当前结论；P16 是否真正接入驱动电路仍未确认。P13 进入较大的铜面，P8-P12 是连续的独立窄线，P14/P15 更像模拟/面板节点。
- 这只支持“排线拓扑候选”，不能单独识别控制器型号。原机主板已确认不在，无法从主板追踪供电和初始化波形。

### 3.2 弱偏置和静态供电证据

早期为限制风险，曾用较大串阻进行静态观察：

- `33k` 串 P6、P13 为地时，压降约 `0.15 V`，计算电流约 `4.55 uA`，CV 且稳定；P6 供电后 P7 会建立接近的电位。
- P7 通过 `51k` 接 P13 时，供电电流明显增加。这与“P6 是外部 VDD、P7 是内部 VLH/参考节点”相符，但只是工作假设。
- P1 与 P15 之间约 `11.7 ohm`，因此两者暂按同一未知模拟/面板轨处理，禁止供电。

一次 P7 直接外供的反证实验必须标记为历史数据，不得重做：

| 接法 | P7 | P6 | 串阻压降/估算电流 | 解释 |
|---|---:|---:|---:|---|
| 3.01 V -> 10k -> P7 | 0.90 V | 0.24 V | 2.1 V，约 0.21 mA | 电流增加但 P7 仍很低 |
| 3.01 V -> 4.7k -> P7 | 0.97 V | 0.27 V | 2.02 V，约 0.43 mA | 电流近似翻倍，P7 只升约 70 mV，P7-P6 约 0.7 V |

这种“低电压钳位、随电流变化很小”的特征更像内部二极管/参考节点，不像外部 VDD。P7 供电已拆除，后续保持悬空。

记录一次推断修正：P7 第一次以约 0.9 V 响应时，曾短暂把“P7=VDD、P6=IREF”当作可能映射；换用 4.7k 后电流近似翻倍而 P7 只升约 70 mV，这个假设被推翻，当前仍采用“P6=外部 VDD 候选、P7=内部参考/钳位候选”。

### 3.3 P11 接线纠正与 4.7k 静态检查

中间曾把 P11 错接到 P6，读到 P11 约 2.83 V；这不是屏幕主动输出的证据。断电改为独立 `P11 -- 51k -- P13` 后，得到：

```text
实验电源对 P13       = 3.01 V
P6 对 P13            = 2.86 V
P8/P9/P10/P12 对 P13 = 约 2.83 V
P11 对 P13           = 0 V
4.7k 压降            = 0.13 V
电源                 = CV，稳定，无异常
```

这组数据只通过“静态接线不明显拖垮供电”的门槛。它不是 ACK 或引脚功能证明。后续记录中所有 4.7k 数值都必须与 2.2k 数值分开。

测量扰动也曾被观察到：有一次直接读 P6 得到 `2.97 V`，但 `3.01 - 2.97 = 0.04 V` 与同轮直接测得的 4.7k 压降 `0.13 V` 不相容。移动表笔后读数会在两个稳定状态间切换，故后续以直接测串阻压降得到的保守电流/电压为准；单次 `2.97 V` 不单独当作永久基线。

### 3.4 受限单线/双线开漏负载测试

测试固件使用完整 `gpio_config()`，只允许开漏拉低并自动释放；串口回读也必须确认 LOW/HIGH。下表合并了历史单线低态/开漏验证轮次（其中 P12/P10/P9 的 2.72/2.7 V 读数来自 4.7k 供电、10k 下拉的不同轮次），具体接线和电源串阻以对应原始日志为准；它们不是同一次连续实验，也不能单独证明候选功能名称。

| 测试 | OLED 节点读数 | P6 供电读数 | 判定和限制 |
|---|---|---|---|
| P12/GPIO5 单线 | `2.72 -> 0.42 -> 2.72 V` | 约 `2.97 -> 2.65 -> 2.86 V`；冷启动后 `2.97 V`，释放后 `2.86 V` 在 30/60 s 稳定 | 拉低和恢复通过；支持 P12 为 RES# 候选，但不是控制器级确认 |
| P10/GPIO3 单线 | `2.72 -> 0.43 -> 2.72 V` | `2.86 -> 2.66 -> 2.86 V` | 开漏路径和恢复通过；只说明可安全做受限低态 |
| P9/GPIO1 单线 | `2.72 -> 0.43 -> 2.72 V` | `2.86 -> 2.65 -> 2.86 V` | 开漏路径和恢复通过；只说明可安全做受限低态 |
| P9+P10 双线（4.7k） | - | `2.86 -> 2.48 -> 2.97 V` | 负载过重且释放后状态异常，不再用于 ACK |
| P9+P10 双线（2.2k） | - | `2.93 -> 2.74 -> 2.93 V`，释放后 30/60 s 稳定 | 当前较好的静态最坏组合；仍不是额定值证明 |

这些测试验证的是“电阻、GPIO、供电路径在低速受限动作下没有明显危险”，不验证 I2C/SPI/并口协议。

### 3.5 0x3C ACK：一次有效 NACK

在 2.2k 方案、P11 保持 LOW、候选分离 SDA 映射下，固定地址固件只做一次：

```text
START -> 0x78（7 位地址 0x3C 的写地址）-> 一个 ACK 时钟 -> STOP
```

串口关键结果摘录（不是完整原始日志）：

```text
ADDRESS: 0x78, bits completed=8/8
START=VERIFIED, STOP=VERIFIED
ACK SAMPLES: P8=HIGH/HIGH, P9=HIGH/HIGH
FINAL RELEASE ... verified=YES
ACK RESULT: NO ACK ON P8/SDAOUT
```

这是“当前接法、当前地址、当前分离 SDA 假设下的有效 NACK”。它不能判定 OLED 损坏。随后断电测得 P8 到 GPIO0 双向约 `10.0/10.1 kOhm`，主要说明外接 10k 串联电阻和通路没有明显断路，不是对 OLED 内部 P8 功能的确认。

固定固件把 P8 作为 SDAOUT 输入、P9 作为 SDAIN/发送侧；即使 P9 出现 LOW，也只能作为诊断，不能算本测试的 ACK。51k 上拉的慢上升沿、错误接口模式、统一 SDA/方向相反、SA0/CS#/BS 状态或缺少模拟供电都仍可能导致 NACK。

### 3.6 0x3D 尝试：无效，不能用于判断地址

在外链对话中曾临时断电改线，把 P11 改为 HIGH 候选后尝试写地址 `0x7A`（7 位 `0x3D`）；该实验已回退，当前仓库固件仍固定 P11 LOW、只支持 0x3C。日志为：

```text
ADDRESS: 0x7A, bits completed=5/8
SEQUENCE FAILURE: address SCL release/rise
ACK SAMPLES: P8=N/A/N/A, P9=N/A/N/A
ACK RESULT: INVALID (sequence/STOP/release not verified)
P11 / GPIO4 / SA0 = HIGH
```

失败发生在地址第 6 位、尚未到达区分 0x3C/0x3D 的 SA0 位；ACK 时钟根本没有执行。因此“0x3D 也没有 ACK”是错误表述，当前只能说 0x3D 尚未被有效测试。P10/SCL 的上升沿问题应先单独查明。

### 3.7 当前固件/仓库状态

- `src/main.cpp`：默认只读锁定版；所有 GPIO 启动、循环和命令前后都恢复为无上下拉高阻输入。
- `src/open_drain_test.cpp`：只允许明确 ARM/CONFIRM 的单线 5 秒低态，及唯一的 P9+P10 双线低态；会验证释放。
- `src/address_ack_test.cpp`：固定 0x3C、显式 RESET 后单次 ACK；不扫描、不重试、不发送初始化或显示数据；终态需要 OLED 与 ESP32 同时断电才能重新开始。
- `platformio.ini`：默认环境仍是 `airm2m_core_esp32c3`，不是 `esp32-c3-devkitm-1`。不要把本地对话中“尝试修改 board”的请求当成已完成修改。
- 早期提交中曾把 P15 当作供电脚、允许扫描或主动探测；这些路径已被后续锁定版本删除，属于过时材料，不能恢复或照做。
- 过去另一个 APRS 项目曾用不同端口/工具链成功刷板，而 OLED 项目的端口或 PlatformIO 版本失败，这只能说明上传链路差异，不能作为 OLED 引脚或屏幕功能证据。
- 当前 Git 工作树除本文件外没有代码改动；本文件是排查日志，不替代项目 README 的编译说明。

## 4. 当前未解决的问题

1. OLED 控制器的确切型号、分辨率和接口模式未确定。
2. P8-P12 的功能名称仍是候选；P12 的复位行为证据较强，但未形成芯片资料闭环。
3. 0x3C 得到有效 NACK；0x3D 测试无效，不能比较两个地址。
4. P10/SCL 曾在 0x3D 流程中 5 ms 内未上升，原因可能是接触、上拉过弱、供电状态或接口假设错误。
5. P1/P14/P15 的模拟/面板功能未知；P7 的内部参考/钳位性质虽有反证支持，仍没有完整数据手册确认。
6. 原机主板不在，无法直接抓取真实初始化时序。

## 5. 下一步：暂停主动协议，做断电微安级指纹

这是当前信息量最高、风险最低的路线。已有工具已足够：0.5 mm FPC 转接板、细探针、夹线、220k/1M 电阻、两块万用表和游标卡尺；暂不需要购买示波器或继续刷固件。

### 5.1 先验证转接板（OLED 不插入）

1. OLED、ESP32、USB、实验电源全部断开。
2. 蜂鸣档只用于空转接板：每个 FPC 触点到对应排针应导通，相邻排针之间不应导通。
3. 确认插入方向没有把 P16/P1 镜像；OLED 插入后不再使用蜂鸣档碰排线。

### 5.2 建立“开路基准”

“开路基准”是测试电源、1M 串阻和万用表自身造成的读数；“开路探针”是 1M 电阻末端悬空、没有碰到任何 OLED 引脚。

```text
0.80 V 电源正极 -- 1M ohm -- 测试点 T（先悬空）
0.80 V 电源负极 ---------------- OLED P13
万用表直流电压档：红笔 T，黑笔 P13
```

电源先确认实际约 0.80 V、CV；1M 电阻尽量靠近被测端，最大电流约 0.8 uA。记录 `Vopen(1 s/10 s/30 s)`。两块表不是必需，第二块只用于监视电源输出；不要使用电流、蜂鸣、二极管或电阻档测带电节点。

### 5.3 逐脚微安测试

断开 ESP32 以及原来的 2.2k、10k、51k 网络；P7 跳过。保持 1M 串阻，每次换脚前关闭输出，测完通过 1M 将该脚接 P13 放电约 30 s。建议顺序：P6、P8、P9、P10、P11、P12、P1、P14、P15。

每脚只记录事实，不先套型号：

```text
引脚：
Vs（电源实际输出）：
Vp 1 s：
Vp 10 s：
Vp 30 s：
CV/CC：
是否稳定：
是否异常：
表笔/夹线是否移动：
```

粗略解释仅作线索：接近开路基准可能是高阻输入/NC；快速稳定在较低值可能是内部钳位；接近 0 V 可能是低阻/地；随时间变化可能与电容、VCOMH 或电荷泵相关。任何单项都不能单独命名引脚。

只有在断电指纹、FPC 走线和资料拓扑互相支持后，才重新设计一个固定、一次性的协议测试。不要先用普通 `Wire`、U8g2 或地址扫描器，因为它们会在假设错误时发送大量无法解释的初始化流量。

## 6. 2026-09-20 首次合并记录

- 来源：外部分享对话、本地 Codex 任务和当前仓库文件。
- 操作：读取并交叉核对 FPC 观察、静态电压、开漏负载、PINS、0x3C/0x3D 日志及当前 PlatformIO 配置。
- 结果：建立了候选引脚表，保留了可复核的原始读数，标出了历史/无效测试，并确定下一步为断电 1M 串阻微安测试。
- 安全状态：本次只编辑 `分析.md`，没有给 OLED 或 P7/P1/P14/P15 上电，没有刷写或修改固件，没有改变仓库代码。
- 维护约定：后续分析必须在本文末尾追加，明确使用的串阻、供电状态、原始串口和判定标签。

## 7. 后续追加模板

每次新分析复制下面模板，追加到本文末尾；保留原始串口输出、照片编号和电阻值。

```markdown
## [YYYY-MM-DD HH:mm TZ] 新记录

- 来源/操作者：
- 目标（只做一件事）：
- 接线图（ASCII）：
- 断电前接线与电阻值：
- 供电设置（电压/限流/串阻）：
- 操作步骤：
- 原始实测（电压、压降、串口、时间点）：
- 计算（如电流/分压）：
- 判定标签：`CONFIRMED_STATIC` / `STRONG_HYPOTHESIS` / `UNCONFIRMED` / `INVALID` / `STOP`
- 这次结果能证明什么：
- 这次结果不能证明什么：
- 当前断电/安全状态：
- 下一步（单一、可回退）：
- 附件或原始材料：
```

### 标签使用规则

- `CONFIRMED_STATIC`：只表示测量条件下的电压/连通/释放事实已复现。
- `STRONG_HYPOTHESIS`：多项独立证据支持，但仍未得到芯片资料或可信协议响应。
- `UNCONFIRMED`：候选映射、型号、地址或接口模式。
- `INVALID`：测试在目标阶段前失败，不能把结果当成阴性结论（本次 0x3D 属于此类）。
- `STOP`：触发安全边界，停止供电或停止当前实验，等待重新审查。

## 2026-09-20 10:21 CST 新分析：是否进入正向微安指纹

- 来源/操作者：当前对话；基于本文已有记录。
- 问题：在 0x3C 有效 NACK、0x3D 无效、控制器和接口仍未知的情况下，是否继续主动协议测试。
- 决策：是。正向微安指纹是当前推荐的下一步，但属于“建议方案”，尚未执行；如果先获得清晰的 FPC 走线或可靠控制器资料，可优先用资料替代本试验。
- 原因：它不需要 ESP32、不发送协议、不依赖 P8/P9/P10/P11/P12 的功能假设；1MΩ 串联把最大注入电流限制在约 0.8 uA，能比较各脚的高阻、钳位、低阻和时间变化特征。
- 前置条件：先验证空 FPC 转接板的触点/相邻脚；然后所有原 2.2k、10k、51k 网络、ESP32 和 USB 均断开；P7 跳过且继续悬空；只使用 0.80 V 电源、1MΩ 串联、P13 回路。
- 第一轮只做开路探针基准，不立即测 OLED 引脚。记录 `Vopen` 的 1 s、10 s、30 s 读数后停下来复核，再逐脚测试。
- 本次没有新增硬件读数，没有刷写固件，也没有改变接线；当前仍应保持断电。
- 下一单一动作：完成空载基准并提交 `Vs / Vopen 1 s / Vopen 10 s / Vopen 30 s / CV/CC / 稳定性`，未复核前不要接触 P6-P16 任一脚。

### 术语补充：Vs、Vopen、Vp

- `Vs`：实验电源正负输出端之间的实际电压。电源设为 0.80 V 后，先用万用表直接测“电源正极对电源负极”；它不经过 1MΩ，也不等于 OLED 引脚电压。
- `Vopen`：1MΩ 后端测试点悬空时，测试点对 P13 的电压。由于万用表输入阻抗有限，`Vopen` 可能低于 `Vs`，这是开路基准的一部分。
- `Vp`：1MΩ 后端接到某个 OLED 引脚后，该引脚对 P13 的电压。

只有一块万用表时，先测 `Vs`，再测 `Vopen` 或 `Vp`；有两块表时可同时监视 `Vs` 和测试点。换接任何引脚前必须关闭电源输出。

`Vopen` 与 `Vp` 必须使用同一个 `Vs`、同一个 1MΩ、同一个量程和同一个 P13 参考点。两者的差异才有意义：

- `Vp ≈ Vopen`：被测脚在该微安偏置下表现为高阻，或没有明显导通路径。
- `Vp` 明显低于 `Vopen`：被测脚存在内部负载、钳位或对地/内部电源的导通路径。
- `Vp` 接近 0 V：更像低阻节点或接地路径，但仍不能单独命名功能。
- `Vp` 在 1 s、10 s、30 s 间变化：可能有电容、储能、电荷泵或测量接触变化，需要复测。

因此，`Vopen` 不是 OLED 的读数，而是“这套电源、1MΩ 和万用表在没有接 OLED 时的基线”；`Vp` 才是接入某个 OLED 引脚后的读数。

## 2026-09-20 18:48 CST 微安基准结果

- 原始读数：`Vs = 0.810 V`；`Vopen(1 s/10 s/30 s) = 0.405/0.405/0.405 V`；`CV/CC = CV`；稳定；未报告异常。
- 初步判定：空载基准可重复且电流很小，但 `Vopen` 恰好约为 `Vs/2`，低于常见 10MΩ 万用表与 1MΩ 串阻约 `0.73 V` 的预期。
- 若接线和电源读数确实同时成立，按分压反推输入等效负载约为 `1 MΩ`，电流约为 `(0.810 - 0.405) / 1 MΩ = 0.405 uA`。这里的等效负载可能是万用表输入阻抗，也可能包含未拆除的并联电阻/夹线/第二块表；不能把它当作 OLED 特征。
- 判定标签：`UNCONFIRMED`（基准稳定，但测试夹具负载尚未解释）。
- 暂停条件：先不要把测试点接到任何 OLED 引脚。
- 下一步：断电后确认测试点只有一只串联 1MΩ，移除任何放电电阻、220k、旧 10k/51k 网络、第二块表或额外夹线；确认 P13 是唯一回路参考。确认万用表处于普通直流电压档（不是二极管档、通断档或 LoZ），用断电电阻档抬起串阻一端核实其实际阻值，并查明表的 DC 输入阻抗，再重复一次 `Vs/Vopen`。若原因明确且仍稳定在约 0.405 V，可把它记录为本套仪表的实际基准，再从 P6 开始逐脚测量。

## 2026-09-20 18:53 CST 串阻标记确认

- 用户确认测试中只有一只标记为 `1004` 的电阻。
- 按标准四位贴片电阻编码，`1004 = 100 × 10^4 ohm = 1.0 MΩ`；这与原方案的串联电阻标称值一致。
- 这排除了“串联电阻明显不是 1MΩ”这一项，但不能排除万用表输入阻抗约 1MΩ、LoZ 模式、额外夹线/第二块表或测试点接错。
- 当前判定仍为 `UNCONFIRMED`；不要把测试点接到 OLED。先确认普通 DC 电压档和表的输入阻抗；若规格不明，可断电将开路测试的串阻临时换为已知 `220kΩ`，在不连接 OLED 的条件下复测。理论上 `Vs≈0.810 V` 时，1MΩ 输入约读 `0.664 V`，10MΩ 输入约读 `0.793 V`；这只是夹具校准，不是 OLED 测试。

## 2026-09-20 记录：200kΩ 开路校准

- 原始读数：使用约 `200kΩ` 串阻时，`Vs = 0.809 V`；`Vopen(1 s/10 s/30 s) = 0.673/0.673/0.673 V`；`CV/CC = CV`；稳定。
- 计算：`Rin ≈ 200kΩ × 0.673 / (0.809 - 0.673) ≈ 0.99MΩ`。这与万用表约 `1MΩ` 等效输入一致。
- 交叉验证：换回 `1MΩ` 串阻时，预期 `Vopen ≈ 0.809 × 0.99 / (1 + 0.99) ≈ 0.403 V`，与此前 `0.405 V` 相符；此前的半电压不是 OLED 负载造成的。
- 判定标签：`CONFIRMED_STATIC`（开路夹具基准已解释；仅限当前万用表、量程和接线）。
- 下一步：断电，把串阻换回标记 `1004` 的 `1MΩ`；确认 ESP32、旧 2.2k/10k/51k 网络和其他引线仍断开，P7 继续悬空。先只把测试点接到 P6，测 `Vp(1 s/10 s/30 s)`；不要同时接其他 OLED 脚。

## 2026-09-20 19:05 CST P6 单脚微安测试接线确认

目标是只观察 P6 在约 0.8 uA 限流下的静态响应，不运行 ESP32、不发送协议。

```text
实验电源正极（约 0.80 V） ── 1MΩ / 1004 ──●── OLED P6
                                             │
                                      测试点 T / Vp

实验电源负极 ─────────────────────────────── OLED P13
万用表 DCV 红表笔 ────────────────────────── 测试点 T（P6 一侧）
万用表 DCV 黑表笔 ────────────────────────── OLED P13
```

接线边界：

- ESP32-C3、USB、ESP32 GND 全部断开，不接 `3V3`，不接任何 GPIO。
- 旧的 2.2k、10k、51k 网络和 200k 校准电阻全部移除；只保留一只标记 `1004` 的 1MΩ 串联电阻。
- P1、P7、P8、P9、P10、P11、P12、P14、P15 全部悬空；P13 只接实验电源负极和万用表黑表笔。
- 红表笔必须接 1MΩ 后端的 P6 节点，不能接 1MΩ 前端的电源正极；黑表笔必须接 P13。
- 实验电源设约 `0.80 V`，可将限流设为 `1 mA`；实际电流由 1MΩ 限制在约 `0.81 uA`，不要用电源电流显示代替电压判断。

操作顺序：断电改线并检查无相邻 FPC 短接；先确认电源端 `Vs`，再上电测 P6 的 `Vp(1 s/10 s/30 s)`。完成后关闭电源，等待放电；若仍有残压，只能用另一只 1MΩ 电阻临时跨 P6-P13 放电，不能直接短接。

## 2026-09-20 19:20 CST P6 正向微安指纹结果

- 原始读数：`P6 Vp(1 s/10 s/30 s) = 0.224/0.224/0.225 V`；`CV/CC = CV`；稳定；无异常。
- 参照：上一轮同一夹具的 `Vs ≈ 0.809 V`、1MΩ 开路基准 `Vopen ≈ 0.405 V`，且万用表等效输入约 `0.99 MΩ`。以下计算以接线确实只有 P6 接入、其余脚悬空为前提。
- 总等效负载（包含万用表）约为 `Rtotal = 1 MΩ × 0.224 / (0.809 - 0.224) ≈ 0.383 MΩ`。
- 由万用表输入负载扣除后，P6 对 P13 的等效负载约 `0.62 MΩ`，对应 P6 支路电流约 `0.36 uA`；这是小信号等效值，不是器件内部真实电阻的证明。
- 判定：`CONFIRMED_STATIC`（P6 在此微安偏置下有稳定、可重复的负载响应）；`STRONG_HYPOTHESIS`（P6 是电气活跃的外部电源/模拟节点候选）。仍不能仅凭此确定 P6 的芯片功能。
- 安全结论：电源没有进入 CC，电压没有持续下滑，没有发热或异常；本次微安测试通过安全门槛。
- 下一步：断电并放电，断开 P6 测试线；保持 ESP32、旧电阻网络和 P7/P1/P14/P15 悬空不变，把同一 1MΩ 测试点只接到 P8，记录 `Vp(1 s/10 s/30 s)`。不要同时保留 P6 接线，也不要测试 P7。

## 2026-09-20 19:30 CST P8 正向微安指纹结果

- 原始读数：`P8 Vp(1 s/10 s/30 s) = 0.400/0.400/0.400 V`；`CV/CC = CV`；稳定；无异常。
- 对照：同一仪表和 1MΩ 串阻的开路基准约 `0.405 V`，差异在当前读数分辨率内，未显示出可辨别的额外直流负载。
- 判定标签：`CONFIRMED_STATIC`（P8 在该微安偏置下的读数稳定且接近开路基准）；`STRONG_HYPOTHESIS`（P8 表现为高阻节点）。这不能确认 P8 一定是 SDAOUT，也不能排除其为处于高阻状态的其他数字/模拟节点。
- 安全结论：没有 CC、持续掉压、发热或异常；本次通过安全门槛。
- 下一步接线：断电并放电，断开 P8 测试线；保持 P6、P7、P1、P14、P15 和其他脚悬空，把同一 1MΩ 测试点只接到 P9，记录 `P9 Vp(1 s/10 s/30 s)`。P8 不能与 P9 同时连接。

```text
实验电源 +0.80 V
        │
        └── 1MΩ / 1004 ──●── OLED P9
                          │
                    万用表红表笔

实验电源负极 ─────────────── OLED P13
万用表黑表笔 ───────────────── OLED P13

OLED P1、P6、P7、P8、P10、P11、P12、P14、P15：悬空
ESP32-C3、USB、GPIO、旧 2.2k/10k/51k 网络：全部断开
```

## 2026-09-20 19:36 CST P9 正向微安指纹结果

- 原始读数：`P9 Vp(1 s/10 s/30 s) = 0.400/0.400/0.400 V`；`CV/CC = CV`；稳定；无异常。
- 对照：读数与同一条件下 `Vopen ≈ 0.405 V` 基本一致，当前分辨率下没有检测到 P9 的额外直流负载。
- 判定标签：`CONFIRMED_STATIC`（P9 的微安偏置响应稳定）；`STRONG_HYPOTHESIS`（P9 表现为高阻节点，和数字信号脚候选相容）。这不能单独确认 P9 是 SDAIN。
- 安全结论：没有 CC、持续掉压、发热或异常；本次通过安全门槛。
- 下一步接线：断电并放电，断开 P9 测试线；保持 P6、P7、P1、P8、P9、P11、P12、P14、P15 悬空，把同一 1MΩ 测试点只接到 P10，记录 `P10 Vp(1 s/10 s/30 s)`。P9 不能与 P10 同时连接。

```text
实验电源 +0.80 V
        │
        └── 1MΩ / 1004 ──●── OLED P10
                          │
                    万用表红表笔

实验电源负极 ─────────────── OLED P13
万用表黑表笔 ───────────────── OLED P13

OLED P1、P6、P7、P8、P9、P11、P12、P14、P15：悬空
ESP32-C3、USB、GPIO、旧 2.2k/10k/51k 网络：全部断开
```

## 2026-09-20 19:54 CST P10 正向微安指纹结果

- 原始读数：`P10 Vp(1 s/10 s/30 s) = 0.400/0.400/0.400 V`；`CV/CC = CV`；稳定；无异常。
- 对照：读数与 `Vopen ≈ 0.405 V` 基本一致，当前分辨率下没有检测到 P10 的额外直流负载。
- 判定标签：`CONFIRMED_STATIC`（P10 的微安偏置响应稳定）；`STRONG_HYPOTHESIS`（P10 表现为高阻节点，和数字时钟脚候选相容）。这不能单独确认 P10 是 SCL。
- 安全结论：没有 CC、持续掉压、发热或异常；本次通过安全门槛。
- 下一步接线：断电并放电，断开 P10 测试线；保持 P6、P7、P1、P8、P9、P10、P12、P14、P15 悬空，把同一 1MΩ 测试点只接到 P11，记录 `P11 Vp(1 s/10 s/30 s)`。P10 不能与 P11 同时连接。

```text
实验电源 +0.80 V
        │
        └── 1MΩ / 1004 ──●── OLED P11
                          │
                    万用表红表笔

实验电源负极 ─────────────── OLED P13
万用表黑表笔 ───────────────── OLED P13

OLED P1、P6、P7、P8、P9、P10、P12、P14、P15：悬空
ESP32-C3、USB、GPIO、旧 2.2k/10k/51k 网络：全部断开
```

## 2026-09-20 19:59 CST P11 正向微安指纹结果

- 原始读数：`P11 Vp(1 s/10 s/30 s) = 0.38/0.39/0.38 V`；`CV/CC = CV`；稳定；无异常。
- 对照：比 `Vopen ≈ 0.405 V` 低约 `15–25 mV`，但没有接近 0 V，也没有随时间持续下降。按当前万用表分辨率和接线误差，这只能记为轻微偏低响应。
- 粗略解释：可能存在弱内部负载/漏电，也可能主要是表笔接触、实际 Vs 或读数分辨率造成；不能把它当作 P11 已被内部拉 LOW，也不能据此确认 SA0。
- 判定标签：`CONFIRMED_STATIC`（读数稳定）；`UNCONFIRMED`（弱负载来源未确定）。
- 安全结论：没有 CC、持续掉压、发热或异常；本次通过安全门槛。
- 下一步接线：断电并放电，断开 P11 测试线；保持 P6、P7、P1、P8、P9、P10、P11、P14、P15 悬空，把同一 1MΩ 测试点只接到 P12，记录 `P12 Vp(1 s/10 s/30 s)`。P11 不能与 P12 同时连接。

```text
实验电源 +0.80 V
        │
        └── 1MΩ / 1004 ──●── OLED P12
                          │
                    万用表红表笔

实验电源负极 ─────────────── OLED P13
万用表黑表笔 ───────────────── OLED P13

OLED P1、P6、P7、P8、P9、P10、P11、P14、P15：悬空
ESP32-C3、USB、GPIO、旧 2.2k/10k/51k 网络：全部断开
```

## 2026-09-20 P11 结果确认与 P12 执行前检查

- 本轮确认：P11 的 `Vp = 0.38/0.39/0.38 V`（1/10/30 s），`CV`、稳定、无异常；相对 `Vopen ≈ 0.405 V` 仅轻微偏低。
- 结论：P11 在当前约 0.8 uA 微安偏置下有稳定读数，但弱负载来源未确定；不能据此确认 P11 是 SA0，也不能把它当作内部 LOW。
- 当前只允许继续做 P12 的单脚微安测试。P11 测试线必须先拆除，P11 与 P12 不得同时连接；P7 仍跳过。

### P12 接线图

```text
实验电源正极（约 0.80 V） ── 1MΩ / 1004 ──●── OLED P12
                                             │
                                       测试点 T / Vp
                                             │
万用表 DCV 红表笔 ──────────────────────────┘

实验电源负极 ─────────────────────────────── OLED P13
万用表 DCV 黑表笔 ────────────────────────── OLED P13

OLED P1、P6、P7、P8、P9、P10、P11、P14、P15：必须悬空
ESP32-C3、USB、3V3/GPIO、旧 2.2k/10k/51k 网络、200k 校准电阻：全部断开
```

### P12 改线和上电顺序

1. 关闭实验电源，拔掉 USB；等待 P12 对 P13 降到接近 0 V。
2. 如仍有残压，只用另一只约 1MΩ 电阻临时跨接 P12-P13 放电约 30 s，禁止直接短接。
3. 拆下 P11 测试线，只保留一只标记 `1004` 的 1MΩ 串阻；确认红表笔在串阻后端测试点、黑表笔在 P13。
4. 逐项确认 P13 只接实验电源负极和黑表笔；其余列出的脚和所有数字/旧电阻网络均悬空或断开。
5. 先直接测电源正负极确认 `Vs` 约 0.80 V，再打开实验电源；不要接 ESP32，也不要发送 `PINS`、`SCAN` 或 `ARM`。
6. 记录 `P12 Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况；完成后先关闭电源并再次放电。

### 后续接线说明格式（用户约定）

后续每一个硬件排查步骤都必须同时给出接线图，至少包含：

1. 电源正、负极和串联电阻的位置；
2. OLED 每个实际连接脚及其对应节点；
3. 万用表红/黑表笔位置；
4. 必须断开的线和必须悬空的脚；
5. 改线前断电、放电和重新上电顺序。

当前下一步 P12 测试的辅助图：

```text
实验电源 +0.80 V
        │
        └── 1MΩ / 1004 ──●── OLED P12
                          │
                     万用表红笔（Vp）

实验电源负极 ─────────────── OLED P13
万用表黑笔 ───────────────── OLED P13

OLED P1、P6、P7、P8、P9、P10、P11、P14、P15：悬空
ESP32-C3、USB、GPIO、旧 2.2k/10k/51k 网络：全部断开
```

## 2026-09-20 P12 正向微安指纹结果

- 原始读数：`P12 Vp(1 s/10 s/30 s) = 0.39/0.39/0.39 V`；`CV/CC = CV`；稳定；无异常。
- 对照：同一套 `1 MΩ / 1004`、万用表和 `Vs≈0.809–0.810 V` 下，`Vopen≈0.405 V`；P12 只低约 `15 mV`，没有出现接近 0 V 或持续下滑的行为。
- 判定标签：`CONFIRMED_STATIC`（读数稳定）；`STRONG_HYPOTHESIS`（P12 在该微安偏置下表现为高阻节点/无明显直流负载）。按分压粗算的弱负载量级约为十几 MΩ，但受万用表分辨率和接触误差影响，不能当作器件真实电阻。
- 功能限制：结果与 `RES#` 候选相容，但没有动态复位或协议响应，不能单独确认 P12 的功能。
- 安全结论：电源保持 CV，读数稳定，无异常；本次通过安全门槛。
- 下一步：断电、放电并拆除 P12 测试线；只把同一 1MΩ 测试点接到 P1，记录 `P1 Vp(1 s/10 s/30 s)`。P12 不得与 P1 同时连接；P7 继续跳过。

### 下一步 P1 接线图

```text
实验电源正极（约 0.80 V） ── 1MΩ / 1004 ──●── OLED P1
                                             │
                                       测试点 T / Vp
                                             │
万用表 DCV 红表笔 ──────────────────────────┘

实验电源负极 ─────────────────────────────── OLED P13
万用表 DCV 黑表笔 ────────────────────────── OLED P13

OLED P6、P7、P8、P9、P10、P11、P12、P14、P15：必须悬空
ESP32-C3、USB、3V3/GPIO、旧 2.2k/10k/51k 网络、200k 校准电阻：全部断开
```

### P1 改线和上电顺序

1. 关闭实验电源并拔掉 USB；等待 P12 对 P13 降到接近 `0 V`。
2. 如仍有残压，只用另一只约 `1 MΩ` 电阻临时跨接 P12-P13 放电约 30 s，禁止直接短接。
3. 拆下 P12 测试线，只保留一只 `1004 = 1 MΩ` 串阻并接到 P1；确认红表笔在串阻后端、黑表笔在 P13。
4. 确认 P13 只接电源负极和黑表笔；P6、P7、P8、P9、P10、P11、P12、P14、P15 全部悬空，ESP32 和旧电阻网络全部断开。
5. 先直接测 `Vs≈0.80 V`，再打开实验电源；若出现 CC、持续掉压、发热、异味或残压异常，立即断电。
6. 记录 `P1 Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况；完成后先关闭电源并放电。

## 2026-09-20 P1 正向微安指纹结果

- 原始读数：`P1 Vp(1 s/10 s/30 s) = 0.404/0.404/0.404 V`；`CV/CC = CV`；稳定；无异常。
- 对照：`Vopen≈0.405 V`，P1 只低约 `1 mV`，在当前仪表分辨率和接触误差内等同于开路基准，没有检测到可辨别的直流负载。
- 重要限制：历史记录显示 P1 与 P15 之间约 `11.7 Ω`，因此本次结果应表述为“P1/P15 耦合节点在微安偏置下的高阻响应”，不能证明 P1 单独是某个功能脚，也不能排除 P15 的耦合影响。
- 判定标签：`CONFIRMED_STATIC`（本次读数稳定、接近基准）；`STRONG_HYPOTHESIS`（P1/P15 耦合节点对 P13 未呈现明显直流导通）。
- 安全结论：电源保持 CV，读数稳定，无异常；本次通过安全门槛。
- 下一步：断电、放电并拆除 P1 测试线，只把同一 1MΩ 测试点接到 P14。P1 和 P15 继续同时悬空，不能与 P14 同时连接；P7 仍跳过。

### 下一步 P14 接线图

```text
实验电源正极（约 0.80 V） ── 1MΩ / 1004 ──●── OLED P14
                                             │
                                       测试点 T / Vp
                                             │
万用表 DCV 红表笔 ──────────────────────────┘

实验电源负极 ─────────────────────────────── OLED P13
万用表 DCV 黑表笔 ────────────────────────── OLED P13

OLED P1、P6、P7、P8、P9、P10、P11、P12、P15：必须悬空
ESP32-C3、USB、3V3/GPIO、旧 2.2k/10k/51k 网络、200k 校准电阻：全部断开
```

### P14 改线和上电顺序

1. 关闭实验电源并拔掉 USB；等待 P1 对 P13 降到接近 `0 V`。
2. 如仍有残压，只用另一只约 `1 MΩ` 电阻临时跨接 P1-P13 放电约 30 s，禁止直接短接。
3. 拆下 P1 测试线，只保留一只 `1004 = 1 MΩ` 串阻并接到 P14；确认红表笔在串阻后端、黑表笔在 P13。
4. 确认 P13 只接电源负极和黑表笔；P1、P6、P7、P8、P9、P10、P11、P12、P15 全部悬空，ESP32 和旧电阻网络全部断开。
5. 先直接测 `Vs≈0.80 V`，再打开实验电源；P1/P15 仍不得接入其他电路。
6. 若出现 `CC`、`Vp` 快速跌落、随时间明显变化、发热、异味或亮点，立即断电，不继续 P15 测试。
7. 正常时记录 `P14 Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况；完成后先关闭电源并放电。

## 2026-09-20 P14 正向微安指纹结果与本轮收尾

- 原始读数：`P14 Vp(1 s/10 s/30 s) = 0.400/0.400/0.400 V`；`CV/CC = CV`；稳定；无异常。
- 对照：同一条件下 `Vopen≈0.405 V`；P14 读数只低约 `5 mV`，没有接近 0 V、持续下滑或时间变化。
- 判定标签：`CONFIRMED_STATIC`（读数稳定）；`STRONG_HYPOTHESIS`（P14 在约 0.8 V、1 MΩ 微安偏置下表现为高阻/无明显直流负载）。这不能确认 P14 的具体芯片功能。
- 安全结论：电源保持 CV，无发热、异味、亮点或其他异常；本次通过安全门槛。

本轮逐脚微安结果汇总：

| 节点 | 读数特征 | 当前解释 |
|---|---|---|
| P6 | 约 `0.224 V` | 明显电气活跃，仍只是 VDD/模拟节点候选 |
| P8、P9、P10、P12 | 约 `0.390–0.400 V` | 接近开路基准，高阻候选 |
| P11 | `0.38/0.39/0.38 V` | 轻微偏低，弱负载来源未确定 |
| P1/P15 耦合组 | P1 `0.404 V`，P1-P15 历史约 `11.7 Ω` | 已覆盖耦合节点，不能拆分解释 |
| P14 | 约 `0.400 V` | 接近开路基准，高阻候选 |

- P15 决策：不再单独加压测试。由于 P1 与 P15 约 `11.7 Ω`，P1 测试时 P15 已通过该低阻关系被间接偏置；再次测试的信息量很低，且会重复触碰未知面板节点。
- P7 决策：继续保持悬空，禁止外部供电。
- 当前安全状态：关闭实验电源，拔掉 USB；所有微安测试线已拆除；P13 不再接电源负极以外的主动电路；P1/P6-P12/P14/P15 均悬空。
- 下一步：结束本轮正向微安指纹，先整理引脚证据和控制器候选，不恢复 ACK、I2C 扫描或主动 GPIO 驱动。若以后要做功能验证，必须另行审查固定映射、受控逻辑电源和开漏时序。

本轮最后一次 P14 测试的接线参考：

```text
实验电源正极（约 0.80 V） ── 1MΩ / 1004 ──●── OLED P14
                                             │
                                       万用表红表笔（Vp）

实验电源负极 ─────────────────────────────── OLED P13
万用表黑表笔 ─────────────────────────────── OLED P13

P1、P6、P7、P8、P9、P10、P11、P12、P15：悬空
ESP32-C3、USB、GPIO、旧 2.2k/10k/51k 网络：断开
```

## 2026-09-20 微安指纹阶段结束后的下一步

### 决策

本轮正向微安指纹已经完成，暂不继续给 P15 或 P7 加压，也不恢复 ACK、I2C 扫描或主动 GPIO 驱动。原因是：

- P8、P9、P10、P12、P14 都接近同一开路基准，微安直流测试无法区分其具体数字功能；
- P11 只有轻微偏低，不能据此命名 SA0；
- P1/P15 已知约 `11.7 Ω` 耦合，P1 测试已近似覆盖该组，单独测试 P15 的信息增益很低；
- 真正的未决问题已经转为控制器型号、接口模式、供电域和固定引脚映射，而不是“是否存在明显直流短路”。

### 现在只做一个动作：保持断电并整理物理证据

```text
实验电源正极 ──（断开）
实验电源负极 ──（断开）

OLED P1、P6、P7、P8、P9、P10、P11、P12、P13、P14、P15：均悬空
ESP32-C3、USB、3V3/GPIO、所有串阻和旧电阻网络：全部断开
```

1. 关闭实验电源，拔掉 USB；如有残压，只用约 `1 MΩ` 电阻放电，禁止直接短接。
2. 拆掉所有测试夹线和 `1004` 串阻，确认 OLED 不再连接任何主动电源。
3. 在断电状态拍摄 FPC 正反面、排线标记、玻璃边缘和驱动 IC 区域的清晰照片；记录照片方向（P1/P16 哪一端）、FPC 宽度/触点间距、玻璃有效区域尺寸和可见丝印。
4. 根据这些资料检索候选控制器数据手册，重点比对 `VDD/VSS/VLL/VLH`、`D0/D1/D2`、`D/C#`、`RES#`、`CS#`、`SA0` 以及接口选择脚的拓扑；不要先把 SSD1306/SSD1312 假设写成结论。
5. 将照片、尺寸、丝印和候选型号发回后，再设计一次固定映射、受控逻辑电源和开漏时序的功能验证。没有资料闭环前，不再刷主动测试固件。

### 当前不要做的事

- 不单独测试 P15；
- 不给 P7、P1、P14、P15 直接外供；
- 不运行普通 `Wire`、U8g2、地址扫描或初始化/清屏程序；
- 不把 `0x3C` 的 NACK 或历史无效 `0x3D` 过程当作控制器损坏结论。

这一阶段的目标是减少“接口假设错误”造成的误判；下一次有新的照片、尺寸、丝印或可信数据手册后，再恢复分析并在本文末尾追加。

## 2026-09-20 FPC 间距、照片和玻璃尺寸补充

- 用户实测 FPC 触点间距：`0.5 mm`。
- 用户实测玻璃尺寸：约 `13 mm × 25 mm`；需注明这是玻璃/有效区域的哪一个边界，当前未给出卡尺照片或测量基准，因此先按用户测量值记录。
- 由尺寸得到的长宽比约 `25/13 = 1.92:1`，接近 `128×64` 的 `2:1` 画幅；这只提高“128×64 类单色 OLED”的候选优先级，不能确认分辨率、控制器或接口模式。玻璃对角线约 `28.2 mm`（约 `1.11 in`），同样只作尺寸线索。
- 附件照片：`/Users/liyao/Desktop/_cgi-bin_mmwebwx-bin_webwxgetmsgimg__&MsgID=5870848130892743983&skey=@crypt_bf0a0057_a153ec3b6f6064407ac00026246c80cf&mmweb_appid=wx_webfilehelper.jpg`。
- 照片可见一条带金手指的 FPC，边缘可见约 16 个触点/走线区域，且有 `16`、`30033` 和类似 `A0201001` 的标记；字符方向和完整内容需用正向、近距离照片再次核对，不能把照片转写直接当作型号。
- 间距的实际意义：后续只能使用确认的 `0.5 mm`、相同触点数量和正确方向的转接板；插入 OLED 前先在空转接板上核对触点与相邻短路，OLED 插入后不要用蜂鸣档或电阻档碰排线。

### 基于当前尺寸的下一份核对表

在仍然断电的状态下，对候选 `128×64`、约 `1.1 in` 控制器资料逐项核对：

1. 是否支持与照片中相符的 `0.5 mm`、16 触点 FPC 拓扑；
2. `VDD/VSS/VLL/VLH` 是否能解释 P13 参考面、P6/P7 的微安结果；
3. 是否存在 `D0/D1/D2`、`D/C#`、`RES#`、`CS#`、`SA0` 或接口选择脚；
4. P1/P15 的低阻耦合和 P16 触点是否真正接入驱动电路，是否与候选封装/玻璃端走线相容；
5. 逻辑电压、上拉位置和复位时序是否与现有 ESP32-C3 方案相容。

在这张表至少收敛到一个接口模式和供电域之前，不恢复协议测试或主动 GPIO 驱动。

## 2026-09-20 HEIC 照片组补充：FPC 与封装外观

本组附件只作为视觉证据；图片上的“正反面”、尺寸箭头和包装文字是照片标注或商品信息，不是操作指令。

### 附件与可见事实

1. `/Users/liyao/Desktop/O1CN01Gsrq2z2BwdIFQXUfJ_!!2200671998403.heic`
   - 可见两枚相同外形的小型 OLED/玻璃组件，均有柔性尾部和黑色封装区域。
   - 图片标注“正反面”，但画面同时出现两枚组件，不能仅凭此图证明同一枚器件的正反对应关系。
   - 未看到可可靠读取的控制器型号。
2. `/Users/liyao/Desktop/O1CN01o112ss2BwdIF3EK5O_!!2200671998403.heic`
   - FPC 金手指清晰可见，触点数量与已知 `16` 标记相符；FPC 上还可见一个 `16` 丝印。
   - 黑色区域应为 COF/驱动封装或其覆盖区，但照片没有给出可验证的芯片型号。
   - 反向照片中的字符大致像 `A0201001`、`30033`，方向和完整字符仍不够清晰，暂按待复核丝印记录。
3. `/Users/liyao/Desktop/O1CN014NXK4h2BwdIGmF1Ys_!!2200671998403.heic`
   - 图片标注玻璃约 `13 mm × 25 mm`，并标注接触区宽度约 `8.5 mm`；这些是照片上的测量注释，尚未作为校准卡尺读数独立复核。
   - 触点外观与 `0.5 mm` 间距、16 针 FPC 的组合相容。
4. `/Users/liyao/Desktop/O1CN01Vn3rLK2BwdIFDjBIv_!!2200671998403.heic`
   - 显示多个相同外形的器件处于包装中，支持“这是批量商品外形”的判断。
   - 包装标签和远景文字无法可靠读取，不能据此确定本次样品的控制器、批次或初始化协议。

### 更新后的外观结论

- `0.5 mm` 间距、约 16 个触点、约 `13 × 25 mm` 玻璃和底部 COF 区域相互一致；“约 1.1 英寸、128×64 类单色 OLED”仍是优先候选，但不是确认结果。
- 照片提供了靠近一端的 `16` 标记，但没有提供 P1/P16 的完整方向闭环、供电域标记、接口模式选择或控制器型号；不能据照片给 P6/P7/P8-P15 重新命名。
- 下一份核对材料应优先寻找：FPC 正向且不旋转的完整丝印、P1/P16 方向标记、玻璃有效区与外框尺寸的测量基准，以及黑色 COF 区域的可读型号/批次信息。
- 在这些信息收敛前，继续保持断电；不得因为外观像 128×64 模块就直接套用 SSD1306、SH1106 或 SSD1312 初始化。

## 2026-09-20 高清 FPC 走线照片补充

新增附件：

- `/Users/liyao/Desktop/_cgi-bin_mmwebwx-bin_webwxgetmsgimg__&MsgID=3156373570914493120&skey=@crypt_bf0a0057_47f504939586e97ead4e4772307fc06e&mmweb_appid=wx_webfilehelper.jpg`
- `/Users/liyao/Desktop/_cgi-bin_mmwebwx-bin_webwxgetmsgimg__&MsgID=3724923433277720075&skey=@crypt_bf0a0057_47f504939586e97ead4e4772307fc06e&mmweb_appid=wx_webfilehelper.jpg`

### 这次照片直接支持的事实

- FPC 接触端清晰显示 16 个金手指，左侧柔性基材上有清楚的 `16` 标记。
- FPC 丝印可读为两行：`30033` 和 `A0201001`。这更像 FPC/批次或内部料号，尚未发现能直接对应控制器型号的文字。
- 16 个接触点的独立走线从接触端向下进入黑色 COF 区域，再分配到玻璃边缘；照片中没有看到可单独识别的外置控制器封装。
- 触点数量与 `0.5 mm` 间距相容：16 个触点的中心跨度理论上为 `(16-1)×0.5 = 7.5 mm`，加上两侧焊盘宽度后接近此前照片标注的约 `8.5 mm`。

### 仍不能从照片确认的内容

- `16` 可能是针数或厂内标记；虽然它位于一端，仍不能仅凭它断言另一端一定是 P1，也不能把当前照片方向直接等同于测量时的 P1/P16 方向。
- 走线的宽度、弯折和汇聚位置不能可靠映射为 VDD、VSS、数据、时钟、D/C、RES 或 SA0；需要无电的转接板方向核对和可信资料交叉验证。
- 黑色 COF 内的驱动芯片型号、接口模式、逻辑电平和初始化时序仍不可见。

### 方向记录（仅用于后续标注）

```text
照片视角：金手指朝上，丝印“16”在左侧

[16 标记端 / 左侧]  ── 16 个 0.5 mm 触点 ──  [另一端 / 编号未确认]
                                  │
                                  └── 独立走线进入 COF 和玻璃
```

当前仍保持断电。后续使用 `0.5 mm / 16 针` 转接板时，先在空转接板上标出“16 标记端”，再核对插座的 1 脚方向；OLED 插入后不要用蜂鸣档或电阻档逐针碰排线。上述照片提高了物理拓扑判断的置信度，但没有改变“暂不恢复协议测试”的决定。

## 2026-09-20 FPC 编号方向确认

- 用户确认：按高清照片的正视方向（16 个金手指朝上、丝印可读），左侧是 `P16`，右侧是 `P1`。
- 因此该视角下的触点顺序为：

```text
左端                                                        右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
```

- 这确认了物理编号方向，不确认任何功能名称、电源域或内部连通关系；从相反面观察或将 FPC 镜像插入转接板时必须重新核对。
- 结合已有测量，P13 仍是共同参考地候选，P6/P7/P8-P15 的功能判断继续沿用证据等级，不因编号方向确认而升级。
- 当前仍保持断电；下一份固定引脚表应使用上述顺序标注，并同时记录转接板的 1 脚/16 脚方向。

## 2026-09-20 转接板编号一致性确认

- 用户确认：FPC 照片中的左侧 `P16`、右侧 `P1` 编号，与所用转接板的编号方向和触点编号一致，没有镜像或倒置。
- 判定标签：`CONFIRMED_STATIC`（机械编号/方向一致）；这不等于已经证明转接板每一条焊盘都导通，也不等于确认 OLED 内部功能。
- 影响：此前记录的 P1、P6、P7、P8-P15 测量标签在物理编号上可以继续沿用，不需要因镜像疑虑重新解释；P16 仍只记录为未确认功能的真实触点。
- 当前安全状态：OLED、实验电源、ESP32、USB、串阻和旧电阻网络继续断开；不要在 OLED 插入状态使用蜂鸣档或电阻档逐针测量。
- 下一步：用已确认的编号建立固定引脚表，逐项对照 FPC 走线和候选控制器数据手册；在接口模式、供电域和逻辑电平收敛前，仍不恢复 ACK 或主动 GPIO 测试。

## 2026-09-20 22:32 CST：结合 `fpc1.jpg` / `fpc2.jpg` 复核 IC 与 FPC，并修正候选针序

### 1. 两张实物图能直接确认的关系

本次使用的原图：

- `/Users/liyao/Documents/github/OLED/pic/fpc1.jpg`
- `/Users/liyao/Documents/github/OLED/pic/fpc2.jpg`

两张图基本是同一面、同一角度，区别主要是焦点和清晰度；它们不是正反面互证。正视金手指、丝印可读时，用户已确认左侧为 `P16`、右侧为 `P1`，且与转接板编号一致：

```text
左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
```

照片中 16 个触点各有一条可见粗铜线向玻璃端延伸；裸露区未见明确断线、相邻并线或外置电阻/电容。靠近玻璃边缘后，粗线进入细间距键合/扇出区，再对应黑色横向驱动区域，并由大量细线继续分配到显示电极。可用下面的层级图理解：

```text
16 针金手指（外部供电/控制接口）
P16 ... P1
    │
    │ 16 条独立可见粗铜线
    ▼
FPC 与玻璃的细间距键合/扇出区
    │
    ▼
黑色横向驱动区域（COG/封装覆盖区候选）
    │
    │ 大量行列细线
    ▼
OLED 显示电极区
```

照片不足以区分黑色区域中裸芯片、保护胶和 ACF 键合边界的精确位置，因此本文统一称“驱动/键合区”，不把它强行写成已确认的 COF 或 COG 结构。

按可见弯折和扇出形态，可作中低置信度的几何分组：

```text
P16 ｜ P15-P13 ｜ P12-P8 ｜ P7-P6 ｜ P5-P1
```

这只表示布线簇；单凭照片不能给任何一脚命名，也不能看穿黑胶下的内部连接。

### 2. 新发现的高度匹配公开规格

GoldenMorning 的 `E109XI-W-C16-03` 原始单页规格书与 KS Display 的 `KSE64128-22` 页面/图纸给出同一组关键特征：

- 1.09 英寸、`64 x 128`、SSD1312、SPI/IIC；
- 16 针 FPC，`P0.5 x (16-1) = 7.5 mm`，尾部宽约 `8.5 mm`；
- 正视图左侧明确标 `16`、右侧标 `1`，与实物和转接板方向一致；
- 面板外形约 `14.0 x 31.96 mm`；偏振片约 `13.4 x 28.3 mm`；有效显示区约 `10.86 x 25.58 mm`。

公开原始材料：

- `https://goldenmorninglcd.com/wp-content/uploads/2025/06/E109XI-W-C16-03-V1.pdf`（第 1 页，V1，2025-04-08）
- `https://goldenmorninglcd.com/oled-display/1.09-inch-oled-64x128-ssd1312-e109xi-w-c16-03/`
- `https://ksdisplay.com/mono-oled-display/1.09-inch-oled-display-64x128-spi-i2c-ssd1312-kse64128-22/`

此前“玻璃约 `13 x 25 mm`”更接近“偏振片宽度 + 有效显示区高度”的混合边界，而不是上述公版的完整玻璃/面板外形；需要以后重新按外框边缘测量，不能用该读数直接排除这个候选。

该公版的 16 针顺序为：

| 引脚 | 公开规格针名 | 当前状态 |
|---:|---|---|
| P1 | C2P | 强候选，未确认 |
| P2 | C2N | 强候选，未确认 |
| P3 | C1P | 强候选，未确认 |
| P4 | C1N | 强候选，未确认 |
| P5 | VDD2 (VBAT) | 强候选，未确认 |
| P6 | VPP (VCC) | 强候选，未确认 |
| P7 | VCOMH | 强候选，未确认 |
| P8 | IREF | 强候选，未确认 |
| P9 | VSS | 强候选，未确认 |
| P10 | IM1 | 强候选，未确认 |
| P11 | CS | 强候选，未确认 |
| P12 | DC | 强候选，未确认 |
| P13 | RES | 强候选，未确认 |
| P14 | VDD1 | 强候选，未确认 |
| P15 | SCL | 强候选，未确认 |
| P16 | SDA | 强候选，未确认 |

这个针序与照片中的几何簇有明显对应：

```text
P16          ：SDA 候选，边缘单线
P15-P13      ：SCL / VDD1 / RES 候选簇
P12-P8       ：DC / CS / IM1 / VSS / IREF 候选簇
P7-P6        ：VCOMH / VPP 模拟高压候选簇
P5-P1        ：VBAT + 两组飞跨电容候选簇
```

它也能解释部分既有现象：P6 的微安响应明显、P7 曾表现出钳位特征、P8 接近高阻，都分别与 `VPP/VCOMH/IREF` 候选相容；此前把 P8-P12 当总线、把 P13 当地进行 0x3C ACK，自然可能一直无响应。

### 3. 重大证据冲突与安全修正

这个型号仍不能定案，原因包括：

- 实物丝印是 `30033 / A0201001`，公开图纸中没有这两个编号；
- 历史测得 `P1-P15` 约 `11.7 ohm`，与“P1=C2P、P15=SCL”不相容，必须以后在完全拆除旧网络后复核测量条件；
- 用户原测尺寸没有按完整面板外框重新量取；
- 照片看不到驱动芯片激光字或黑胶下连接。

因此判定标签为 `STRONG_HYPOTHESIS`，不是 `CONFIRMED`。但它足以改变安全边界：

1. 原来所有以 P13 为负端/参考点得到的电压值仍是有效的“Pxx 对 P13”原始事实，但不再支持 `P13=VSS`。
2. 暂停原计划的 `P5 对 P13` 微安测试；不得继续把 P13 接电源负极、ESP32 GND 或总线地。
3. 暂停旧映射下的 ACK、I2C、U8g2、复位和主动 GPIO；旧 `P8/P9/P10/P11/P12` 数字脚命名整体降级为历史假设。
4. P6/P7 继续禁止直接外供；P1-P5 也不搭飞跨电容或电荷泵，直到候选针序得到进一步验证。

### 4. 下一单一动作：微安验证 `P14(VDD1)` 对 `P9(VSS)` 候选

只做一个新的差分指纹，不接 ESP32、不发送协议。最大注入电流由 `0.80 V / 1 Mohm` 限制在约 `0.8 uA`。

#### 引脚位置辅助图

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
          ▲                         ▲
          │                         └── 电源负极 + 万用表黑笔
          └── 1 Mohm 后端 + 万用表红笔

P14 = 从左数第 3 脚
P9  = 从左数第 8 脚
```

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P14
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P9
万用表 DCV 黑表笔 ───────────────────────────── OLED P9

OLED P1-P8、P10-P13、P15、P16：全部悬空
特别注意：P13 本次必须悬空
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

#### 断电、校准、上电和收尾顺序

1. 关闭实验电源、拔掉 ESP32 USB，拆掉所有旧测试线；确认没有任何脚仍接 P13 作为地。
2. 如存在残压，只用另一只约 `1 Mohm` 电阻临时跨接 P14-P9 放电约 30 s；禁止直接短接。
3. 先接电源负极和黑表笔到 P9；正极经唯一一只 `1004 = 1 Mohm` 到测试点 T，T 暂时悬空，红表笔接 T。
4. 电源设约 `0.80 V`，先复测 `Vs` 和 T 悬空时的 `Vopen(1 s/10 s/30 s)`；正常应接近本套仪表既有基准 `0.405 V`。若基准明显变化，停止，不接 P14。
5. 关闭输出；确认 P14 是左起第 3 脚、P9 是左起第 8 脚，再把 T 只接到 P14。
6. 重新打开输出，记录 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。若出现 CC、持续掉压、跳变、发热、异味或亮点，立即断电。
7. 测完关闭输出，拆下 P14 测试线，再用约 `1 Mohm` 跨接 P14-P9 放电约 30 s，最后全部悬空。

请按下面格式返回：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P14 对 P9：Vp 1 s =
P14 对 P9：Vp 10 s =
P14 对 P9：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

解释边界：若 `Vp` 明显低于同轮 `Vopen`，会支持 P14-P9 之间存在内部供电负载；若 `Vp` 仍接近 `Vopen`，只能说明 0.8 V 微安条件下呈高阻，不能据此否定候选型号。

## 2026-09-20：P14 对 P9 正向微安指纹结果

- 原始读数：`Vopen = 0.41 V`；`P14 对 P9：Vp(1 s/10 s/30 s) = 0.40/0.40/0.40 V`。
- 时间特征：1 s、10 s、30 s 完全一致，没有可见充电爬升或持续下滑。
- 相对基准：`Vp` 比同轮 `Vopen` 低约 `0.01 V`，仅是很小偏差；在当前两位小数读数、接触误差和约 `1 Mohm` 万用表输入负载下，应归为“接近开路基准”。
- 粗略量级：若沿用此前 `Vs≈0.809 V` 和万用表约 `0.99 Mohm` 的校准，额外内部负载约为数十兆欧量级；由于本轮没有同时报告精确 `Vs`，该值只能说明负载很弱，不能作为器件真实电阻。
- 判定标签：`CONFIRMED_STATIC`（本轮读数稳定）；`UNCONFIRMED`（P14=VDD1、P9=VSS 的功能仍未确认）。
- 能证明：P14-P9 在约 0.8 V、1 Mohm 限流条件下没有明显低阻或强钳位。
- 不能证明：不能因其接近开路就排除 VDD1/VSS；未达到候选逻辑电源的正常工作电压时，内部 CMOS 可能仍呈高阻。
- 本轮尚未报告 `CV/CC`、整体稳定性和异常情况；只有在确认仍为 `CV`、稳定、无发热/异味/亮点后，才可继续下一项。

### 下一单一动作：P6 对 P9 微安指纹

目的：在新候选针序中 P6 是 `VPP(VCC)`、P9 是 `VSS`；而历史上 P6 对旧参考 P13 曾明显低于开路基准。改用 P9 作为唯一参考重测 P6，可以区分“P6 对真正地存在内部模拟路径”与“先前读数主要来自 P13/RES 相关路径”。本试验仍不等于给 OLED 正常供电。

#### 引脚位置

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
                                  ▲           ▲
                                  │           └── 1 Mohm 后端 + 红表笔
                                  └── 电源负极 + 黑表笔

P9 = 从左数第 8 脚
P6 = 从左数第 11 脚，也就是从右数第 6 脚
```

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P6
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P9
万用表 DCV 黑表笔 ───────────────────────────── OLED P9

OLED P1-P5、P7-P8、P10-P16：全部悬空
特别注意：P13、P14 本次都必须悬空
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

#### 改线和测量顺序

1. 关闭实验电源并拔掉 USB；先用约 `1 Mohm` 临时跨接 P14-P9 放电约 30 s，然后拆除 P14 测试线。
2. 确认 P13、P14 和除 P6/P9 外的所有 OLED 脚全部悬空；P6、P9 不得通过旧网络连接其他脚。
3. 电源负极和黑表笔只接 P9；电源正极经唯一一只 `1004 = 1 Mohm` 到测试点 T，红表笔接 T。
4. T 先悬空，复测 `Vs` 与 `Vopen(1 s/10 s/30 s)`；若 `Vopen` 不再接近 `0.41 V`，停止并检查接线。
5. 关闭输出，把 T 只接 P6；再次确认 P6 是左起第 11 脚、P9 是左起第 8 脚。
6. 打开输出，记录 P6 对 P9 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。
7. 出现 CC、持续掉压、跳变、发热、异味或亮点时立即断电；正常测完也先关闭输出，再用约 `1 Mohm` 跨接 P6-P9 放电约 30 s，最后全部悬空。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P6 对 P9：Vp 1 s =
P6 对 P9：Vp 10 s =
P6 对 P9：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-20：P6 对 P9 正向微安指纹结果

- 原始读数：`Vopen = 0.405 V`；`P6 对 P9：Vp(1 s) = 0.322 V`，`Vp(30 s) = 0.322 V`。
- 本轮未单独报告 `Vp(10 s)`、精确 `Vs`、`CV/CC` 和异常情况；1 s 与 30 s 相同，至少说明两个端点之间没有可见漂移。
- 相对基准：P6 接入后比开路基准低 `0.083 V`，约为基准的 `20.5%`，明显大于此前 P14-P9 的 `0.01 V` 小偏差，不能归入普通读数误差。
- 若沿用此前 `Vs≈0.809-0.810 V`、串阻 `1 Mohm`、万用表输入约 `0.99-1.00 Mohm` 的校准，P6-P9 额外内部负载粗略约为 `1.9 Mohm`，OLED 分流电流约 `0.16-0.17 uA`。该计算只用于比较指纹，不是器件额定电阻。
- 判定标签：`CONFIRMED_STATIC`（P6-P9 存在稳定弱负载）；`STRONG_HYPOTHESIS`（与 P6=VPP/VCC、P9=VSS 候选相容，但尚未由功能响应确认）。
- 与历史结果的关系：P6 对旧参考 P13 曾约 `0.224 V`，而本轮 P6 对 P9 为 `0.322 V`；说明 P6 对两个节点的内部路径强弱不同。若新候选正确，P13 是 RES，较强响应可能来自复位/保护网络，不能再据此把 P13 当地。
- 安全条件：只有在用户确认本轮仍为 `CV`、稳定且无发热、异味或亮点时，才进入下一项；否则立即保持断电并停止。

### 下一单一动作：P5 对 P9 微安指纹

目的：新候选针序中 P5 是 `VDD2(VBAT)`、P9 是 `VSS`。在完全不接 P1-P4 飞跨电容、且只允许最大约 `0.8 uA` 的条件下，比较 P5-P9 与 P6-P9 的指纹，可继续检验右侧 `P5-P1` 是否像电源/电荷泵簇。本试验不是正常上电，也不得连接 P1-P4。

#### 引脚位置

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
                                  ▲               ▲
                                  │               └── 1 Mohm 后端 + 红表笔
                                  └── 电源负极 + 黑表笔

P9 = 从左数第 8 脚
P5 = 从左数第 12 脚，也就是从右数第 5 脚
```

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P5
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P9
万用表 DCV 黑表笔 ───────────────────────────── OLED P9

OLED P1-P4、P6-P8、P10-P16：全部悬空
特别注意：P6、P7、P13、P14 本次都必须悬空
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

#### 改线和测量顺序

1. 关闭实验电源并拔掉 USB；先用约 `1 Mohm` 临时跨接 P6-P9 放电约 30 s，然后拆除 P6 测试线。
2. 确认 P1-P4 没有安装任何飞跨电容，P6/P7/P13/P14 及其他非测试脚全部悬空。
3. 电源负极和黑表笔只接 P9；电源正极经唯一一只 `1004 = 1 Mohm` 到测试点 T，红表笔接 T。
4. T 先悬空，复测 `Vs` 与 `Vopen(1 s/10 s/30 s)`；若 `Vopen` 不再接近 `0.405 V`，停止并检查接线。
5. 关闭输出，把 T 只接 P5；再次确认 P5 是左起第 12 脚、P9 是左起第 8 脚。
6. 打开输出，记录 P5 对 P9 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。
7. 出现 CC、持续掉压、跳变、发热、异味或亮点时立即断电；正常测完也先关闭输出，再用约 `1 Mohm` 跨接 P5-P9 放电约 30 s，最后全部悬空。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P5 对 P9：Vp 1 s =
P5 对 P9：Vp 10 s =
P5 对 P9：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-20：P5 对 P9 正向微安指纹结果

- 原始读数：`Vopen = 0.405 V`；`P5 对 P9：Vp(1 s) = 0.392 V`，`Vp(30 s) = 0.390 V`。
- 时间特征：29 s 内只下降约 `0.002 V`，属于轻微缓慢变化；建议视为“基本稳定但有弱 settling”，不能写成完全无变化。
- 相对基准：P5 接入后比开路基准低 `0.013-0.015 V`，明显小于 P6-P9 的 `0.083 V`，因此 P5-P9 更接近高阻，仅存在很弱的内部负载。
- 若沿用此前 `Vs≈0.809-0.810 V`、串阻 `1 Mohm`、万用表输入约 `1 Mohm` 的校准，P5-P9 额外内部负载粗略约为 `13-15 Mohm`，OLED 分流电流约 `0.026-0.030 uA`。该计算只用于横向比较。
- 判定标签：`CONFIRMED_STATIC`（P5-P9 为弱负载、接近高阻）；`STRONG_HYPOTHESIS`（P5 与 P6 呈现不同指纹，与 VDD2/VBAT 输入和 VPP/VCC 模拟输出为不同节点相容，但不能单独确认功能）。
- 本轮仍未报告精确 `Vs`、`Vp(10 s)`、`CV/CC` 和异常情况；只有确认电源保持 CV、无发热/异味/亮点时才继续。

### 下一单一动作：直接区分旧 P13 地假设与新 P9 地候选

目的：旧映射把 P13 当作 VSS；新 SSD1312 公版候选把 P9 定义为 VSS、P13 定义为 RES。用 `0.80 V + 1 Mohm` 测 P13 对 P9，可在不正常供电、不接 GPIO 的情况下直接检查两脚是否近似同一低阻地节点。

判读原则：

- 若 `P13 对 P9` 的 `Vp` 接近 `0 V`，才支持两脚之间存在低阻/共同地路径；
- 若 `Vp` 接近 `Vopen≈0.405 V`，则强烈反对“P13 是直接地”，并与 P13=RES 高阻输入候选相容；
- 若落在中间，只记录内部钳位/偏置路径，不能单凭一次读数命名。

#### 引脚位置

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
               ▲                  ▲
               │                  └── 电源负极 + 黑表笔
               └── 1 Mohm 后端 + 红表笔

P13 = 从左数第 4 脚
P9  = 从左数第 8 脚
```

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P13
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P9
万用表 DCV 黑表笔 ───────────────────────────── OLED P9

OLED P1-P8、P10-P12、P14-P16：全部悬空
特别注意：P5、P6、P7、P14 本次都必须悬空
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

#### 改线和测量顺序

1. 关闭实验电源并拔掉 USB；先用约 `1 Mohm` 临时跨接 P5-P9 放电约 30 s，然后拆除 P5 测试线。
2. 确认 P13 已与所有历史地线、夹线和旧网络完全断开；除 P9 参考线外，OLED 当前不得有第二条接地线。
3. 电源负极和黑表笔只接 P9；电源正极经唯一一只 `1004 = 1 Mohm` 到测试点 T，红表笔接 T。
4. T 先悬空，复测 `Vs` 与 `Vopen(1 s/10 s/30 s)`；若 `Vopen` 不接近 `0.405 V`，停止并检查接线。
5. 关闭输出，把 T 只接 P13；再次确认 P13 是左起第 4 脚、P9 是左起第 8 脚。
6. 打开输出，记录 P13 对 P9 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。
7. 出现 CC、持续掉压、跳变、发热、异味或亮点时立即断电；正常测完也先关闭输出，再用约 `1 Mohm` 跨接 P13-P9 放电约 30 s，最后全部悬空。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P13 对 P9：Vp 1 s =
P13 对 P9：Vp 10 s =
P13 对 P9：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-20：P13 正、P9 负的微安指纹结果

- 原始接法：电源正极经 `1 Mohm` 接 P13，电源负极接 P9；万用表红笔 P13、黑笔 P9。
- 原始读数：`P13 对 P9：Vp(1 s/10 s/30 s) = 0.280/0.280/0.280 V`；`CV/CC = CV`；稳定；无异常。
- 对照：同轮开路基准沿用约 `0.405 V`。接入 OLED 后下降约 `0.125 V`，约为开路基准的 `30.9%`，属于明显而稳定的内部导通/钳位，不是读数噪声。
- 若沿用 `Vs≈0.809-0.810 V` 和万用表约 `1 Mohm` 的校准，该工作点可粗略等效为 OLED 在 P13-P9 间增加约 `1.1 Mohm` 的负载，分流约 `0.25 uA`；若实际路径是保护二极管，此数值不是线性电阻。
- 重要判定：`0.280 V` 既不接近 0 V，也不接近 `Vopen`，所以不能把 P13/P9 判成同一低阻地，也不能只凭这一极性确定哪一脚是 VSS。
- 方向歧义：若旧映射 `P13=VSS、P9=输入` 正确，本次“P13 正、P9 负”可能正向偏置 P9 的下侧保护二极管；若新映射 `P9=VSS、P13=RES` 正确，也可能是 RES 内部偏置/下拉网络。必须交换极性做成对比较。
- 判定标签：`CONFIRMED_STATIC`（该方向存在稳定钳位）；`UNCONFIRMED`（P9/P13 哪一脚是地仍未由本次单向结果解决）。

### 下一单一动作：交换极性的 P9 正、P13 负微安指纹

目的：保持相同电压、同一 `1 Mohm`、同一万用表，只交换 P9/P13 的正负方向。两个方向若明显不对称，更像输入保护二极管/偏置网络；钳位更强的方向可用于判断哪一脚更像地，但仍需结合公开针序解释。

#### 引脚位置

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
               ▲                  ▲
               │                  └── 1 Mohm 后端 + 红表笔（本轮正端）
               └── 电源负极 + 黑表笔（本轮负端）

P13 = 从左数第 4 脚
P9  = 从左数第 8 脚
```

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P9
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P13
万用表 DCV 黑表笔 ───────────────────────────── OLED P13

OLED P1-P8、P10-P12、P14-P16：全部悬空
P5、P6、P7、P14 继续悬空
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

#### 换向和测量顺序

1. 关闭实验电源；先用约 `1 Mohm` 跨接 P13-P9 放电约 30 s，然后拆除当前两条电源/表笔连接。
2. 将电源负极和万用表黑笔改接 P13；电源正极仍经唯一一只 `1004 = 1 Mohm` 到测试点 T，红表笔接 T。
3. T 暂时悬空，重新记录本方向的 `Vs` 与 `Vopen(1 s/10 s/30 s)`；应仍接近 `0.405 V`。
4. 关闭输出，把 T 只接 P9；复核 P9 是左起第 8 脚、P13 是左起第 4 脚。
5. 打开输出，记录“P9 正、P13 负”的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。
6. 出现 CC、持续掉压、跳变、发热、异味或亮点时立即断电；正常结束也先关闭输出，再用约 `1 Mohm` 跨接 P9-P13 放电 30 s，最后全部悬空。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P9 正、P13 负：Vp 1 s =
P9 正、P13 负：Vp 10 s =
P9 正、P13 负：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-20：P9 正、P13 负的反向微安指纹结果与候选修正

- 原始接法：电源正极经 `1 Mohm` 接 P9，电源负极接 P13；万用表红笔 P9、黑笔 P13。
- 原始读数：`Vopen = 0.400 V`；`P9 对 P13：Vp(1 s/10 s/30 s) = 0.400/0.400/0.400 V`；`CV/CC = CV`；稳定；无异常。
- 反向结果与同轮开路基准完全一致，说明“P9 正、P13 负”方向在约 0.8 V 微安条件下没有可辨别的直流负载。

与上一方向形成成对比较：

| 极性 | Vopen | Vp | 结果 |
|---|---:|---:|---|
| P13 正、P9 负 | 约 `0.405 V` | `0.280 V` | 明显钳位/导通 |
| P9 正、P13 负 | `0.400 V` | `0.400 V` | 等同开路 |

- 两个方向相差约 `0.120 V`，证明 P13-P9 之间主要是方向性结/保护网络，而不是普通对称电阻，也不是两脚同为低阻地。
- 典型 CMOS 输入的下侧保护二极管方向是“VSS 为阳极、输入脚为阴极”。本次只有“P13 正、P9 负”出现钳位，因此与 `P13=VSS、P9=数字输入` 的旧候选高度相容；它与公开 E109XI/KSE 公版所列 `P9=VSS、P13=RES` 的简单输入保护方向相反。
- 该方向证据不能单独达到芯片级确认，因为未知芯片可能含内部上拉/下拉或特殊复位网络；但结合此前 P13 作为共同参考时 P8-P12 正向均接近高阻、P6 明显活跃等数据，当前应恢复 `P13=VSS` 为领先工作假设。
- 候选等级修正：`E109XI-W-C16-03 / KSE64128-22` 继续保留为高度相似的机械尺寸/16 针公版参考，但其具体针脚表从 `STRONG_HYPOTHESIS` 降为 `CONFLICTING_REFERENCE`，不得再按其 P9 地针序上电。
- 安全修正：立即停止所有以 P9 为电源负极/地的后续测试；P5/P6 对 P9 的读数仅保留为两节点之间的指纹，不能再解释为“对 VSS”。ESP32、协议和主动 GPIO 仍保持暂停。
- 判定标签：`CONFIRMED_STATIC`（P13-P9 存在强方向不对称）；`STRONG_HYPOTHESIS`（P13=VSS、P9=输入重新成为领先解释）；具体 P9 功能仍未确认。

### 下一单一动作：用 P10 做第二个方向性复核

目的：历史上“P10 正、P13 负”约为 `0.400 V`、接近开路；现在只测相反方向“P13 正、P10 负”。如果也出现与 P9 相似的单向钳位，将形成第二个独立数字脚证据，显著增强 P13=VSS，而无需提高电压或连接 GPIO。

#### 引脚位置

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
               ▲              ▲
               │              └── 电源负极 + 黑表笔
               └── 1 Mohm 后端 + 红表笔

P13 = 从左数第 4 脚
P10 = 从左数第 7 脚
```

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P13
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P10
万用表 DCV 黑表笔 ───────────────────────────── OLED P10

OLED P1-P9、P11-P16：全部悬空
特别注意：P9 本次必须悬空，不再作为地
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

#### 改线和测量顺序

1. 关闭实验电源；先用约 `1 Mohm` 跨接 P9-P13 放电约 30 s，然后拆除 P9 测试线。
2. 将电源负极和万用表黑笔接 P10；电源正极仍经唯一一只 `1004 = 1 Mohm` 到测试点 T，红表笔接 T。
3. T 暂时悬空，记录 `Vs` 与 `Vopen(1 s/10 s/30 s)`；应仍约 `0.400-0.405 V`。
4. 关闭输出，把 T 只接 P13；确认 P13 是左起第 4 脚、P10 是左起第 7 脚，P9 已悬空。
5. 打开输出，记录“P13 正、P10 负”的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。
6. 正常结束也先关闭输出，再用约 `1 Mohm` 跨接 P13-P10 放电 30 s，最后全部悬空；出现 CC、持续掉压、跳变、发热、异味或亮点时立即断电。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P13 正、P10 负：Vp 1 s =
P13 正、P10 负：Vp 10 s =
P13 正、P10 负：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-20：P13 正、P10 负的第二组方向性复核结果

- 原始读数：`Vopen = 0.400 V`；`P13 正、P10 负：Vp(1 s/10 s/30 s) = 0.280/0.280/0.280 V`；`CV/CC = CV`；稳定；无异常。
- 历史同条件正向记录为“P10 正、P13 负约 `0.400 V`、接近开路”；本轮交换方向后稳定在 `0.280 V`。该不对称与 P9-P13 的成对结果完全复现。

两组独立信号脚汇总：

| 信号候选 | 信号脚正、P13负 | P13正、信号脚负 | 方向性 |
|---|---:|---:|---|
| P9 | `0.400 V`，等同开路 | `0.280 V`，明显钳位 | 强不对称 |
| P10 | 历史约 `0.400 V`，接近开路 | `0.280 V`，明显钳位 | 强不对称 |

- 两个彼此独立的引脚出现同样的保护结方向，最简单且与常见 CMOS I/O 一致的解释是：P13 是公共 VSS，P9/P10 是共享该 VSS 保护网络的数字 I/O。
- 判定升级：`P13=VSS` 从工作假设升级为 `STRONG_HYPOTHESIS`（很强电气证据，尚缺芯片资料/直接低阻结构确认）；`P9/P10=数字信号组` 同样获得强支持，但两者具体是 SDA/SCL/D0/D1 仍未由本轮区分。
- 公开 E109XI/KSE 的 `P9=VSS、P13=RES` 针序与两组方向性数据冲突，因此不能再作为本屏具体针序；它只保留为 1.09 英寸、16针0.5mm、机械外形相似的参考家族。
- 旧 0x3C NACK 也不能再用“地脚接错”来解释；它仍可能来自接口模式、分离 SDA、供电域、电荷泵/面板高压、地址或时序问题。

### 当前决策：暂停新增上电，先找完全匹配的针脚资料

当前再重复 P8/P11/P12 的同类保护二极管测试，只会继续证明它们属于输入组，无法区分数据、时钟、D/C、RES 或接口选择。信息瓶颈已经回到“确切控制器/玻璃针序”，因此先停止硬件注入，按以下强候选序列检索：

```text
P5  = VBAT 候选
P6  = VDD 候选（对 P13 的微安响应最活跃）
P7  = VCOMH/内部模拟节点候选
P8  = D2/SDAOUT 候选
P9  = D1/SDAIN 候选
P10 = D0/SCL 候选
P11 = D/C#/SA0 候选
P12 = RES# 候选
P13 = VSS 强候选
P14 = IREF 候选
P15 = VCC/VPP 候选
P16 = 未确认真实触点
```

本阶段安全接线图：

```text
实验电源正极 ──（断开）
实验电源负极 ──（断开）
万用表红/黑表笔 ──（均移开）

OLED P1-P16：全部悬空
ESP32-C3、USB、GPIO、3V3、所有串阻和旧电阻网络：全部断开
```

断电收尾：关闭输出后，用约 `1 Mohm` 跨接 P13-P10 放电 30 s，再拆除所有测试线。没有找到针序完全匹配的正式资料前，不搭 P1-P4 飞跨电容、不直接供 P7/P15、不恢复 ACK/I2C/U8g2 或主动 GPIO。

### 公开型号检索阶段结果

- 截至本轮结束，尚未找到同时满足“约 13 x 25 mm 可见区域/小型竖屏、16 针 0.5 mm、并与当前候选逐脚序列一致”的公开正式型号。
- 已确认机械相近的 `E109XI-W-C16-03 / KSE64128-22` 使用另一套针序，且与 P9/P10 对 P13 的方向性实测冲突，不能套用。
- 公开 Ronbo 产品族中，机械或针数相近的若干型号也不能闭环：`RB096-6428TSWIG08` 为 14 针；`RB130-2864KSWXG22`、`RB129-6428TSWWG03` 的玻璃明显更大；`RB042-7240TSWPG10` 又明显过小。
- 当前候选序列更接近部分 16 针 SH1106/CH1115/SSD13xx 裸屏家族，但没有型号资料闭环，控制器名称继续保持 `UNCONFIRMED`。
- 当前最有价值的新结论不是控制器型号，而是 P13 作为 VSS 的方向性证据已由 P9、P10 两个独立引脚复现；下一轮如继续微安分类，应优先重测“P5 正、P13 负”，因为此前 P5-P9 使用了已被否定的地参考。但本轮先保持全部断电，等待明确继续指令。

### 下一单一动作：以 P13 为参考重测 P5

目的：P5-P9 的旧读数使用了被否定的 P9 地假设，不能判断 P5 是否为 VBAT。现在只测“P5 正、P13 负”，仍限制最大电流约 `0.8 uA`。

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
               ▲                                  ▲
               │                                  └── 1 Mohm 后端 + 红表笔
               └── 电源负极 + 黑表笔

实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P5
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── OLED P13
万用表黑表笔 ───────────────────────────────── OLED P13

OLED P1-P4、P6-P12、P14-P16：全部悬空
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

顺序：

1. 关闭输出，用约 `1 Mohm` 跨接 P13-P10 放电 30 s，拆除 P10 线。
2. 电源负极和黑表笔只接 P13；正极经唯一一只 `1 Mohm` 到测试点 T，红笔接 T。
3. T 悬空时记录 `Vs` 与 `Vopen(1 s/10 s/30 s)`；应约 `0.400-0.405 V`。
4. 关闭输出，将 T 只接 P5；确认 P5 是左起第 12 脚、P13 是左起第 4 脚。
5. 重新打开输出，记录 P5 对 P13 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。
6. 测完断电，用约 `1 Mohm` 跨接 P5-P13 放电 30 s，最后全部悬空。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P5 对 P13：Vp 1 s =
P5 对 P13：Vp 10 s =
P5 对 P13：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-20：以 P13 为参考的 P5 正向微安指纹结果

- 原始读数：`Vopen = 0.400 V`；`P5 对 P13：Vp(1 s/10 s/30 s) = 0.350/0.350/0.350 V`；`CV/CC = CV`；稳定；无异常。
- 相对基准：接入 P5 后比同轮开路基准低 `0.050 V`，约为基准的 `12.5%`；差异稳定且明显大于仪表末位波动，说明 P5-P13 之间存在可重复的内部弱负载。
- 若沿用 `Vs≈0.80-0.81 V`、串阻 `1 Mohm`、万用表输入约 `1 Mohm` 的夹具模型，P5-P13 的附加等效负载约为 `3-3.5 Mohm`，OLED 分流约 `0.10-0.11 uA`。该数值只用于同一夹具下比较，不是器件额定电阻。
- 横向比较：P6-P13 曾为约 `0.224 V`，明显强于 P5-P13 的 `0.350 V`；P8-P12/P14 正向对 P13 多接近 `0.38-0.40 V`。因此 P5 的特征位于“数字高阻组”和“P6 强电源/模拟负载”之间。
- 当前解释：该层级关系与 `P5=VBAT/电荷泵输入、P6=VDD 逻辑电源` 的候选结构相容，但单一正向直流指纹不能命名引脚，也不能确认控制器。
- 判定标签：`CONFIRMED_STATIC`（P5-P13 存在稳定弱负载）；`STRONG_HYPOTHESIS`（P5/P6 是不同电源或模拟节点，P13 是 VSS）；具体 `VBAT/VDD` 名称仍为 `UNCONFIRMED`。
- 安全状态：本轮保持 CV、稳定、无异常；测量结束后应关闭输出，用约 `1 Mohm` 跨接 P5-P13 放电约 30 s，再将全部引脚悬空。

### 暂停点

在决定下一项前，不提高电压、不降低串阻、不搭 P1-P4 飞跨电容、不连接 P15/P16、ESP32 或 GPIO。下一项只从以下两种微安级方案中选择一个：

1. 反向测 `P13 正、P5 负`，用方向性判断 P5 更像供电轨还是普通数字输入；
2. 测尚未覆盖的 P16 对 P13，判断这个照片中真实接入驱动区的边缘触点更像数字输入、模拟节点还是近似 NC。

两项不得同时进行；正式接线图在完成信息量/风险复核后再追加。

### 已取消、不得执行：P16 对 P13 的双向微安指纹

> 后续高清图复核确认 P16 铜箔在连接端终止，且用户断电测得 P16-P13 为 `OL`。本节仅保留为历史计划，禁止按本节接线；以文末“P16 物理断线确认与测试取消”为准。

复核决定优先测试 P16，而不是反向测试 P5。理由是 P5 反向出现保护钳位时仍难区分 VBAT、VDD 或普通受保护 PAD；P16 当前候选包含 `NC/高阻边缘脚/有效数字脚`，双向结果的区分力更高。整组测试分 A、B 两相，每相之间必须断电和放电，不能在线交换。

#### 引脚位置

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
 ▲              ▲
 │              └── P13：当前 VSS 强候选
 └── P16：最左侧边缘触点，本轮被测脚
```

#### A 相：P16 正、P13 负

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P16
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P13
万用表 DCV 黑表笔 ───────────────────────────── OLED P13

OLED P1-P12、P14、P15：全部悬空
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

A 相顺序：

1. 关闭输出；先用约 `1 Mohm` 跨接 P5-P13 放电 30 s，拆除 P5 测试线。
2. 电源负极与黑表笔只接 P13；正极经唯一一只 `1004 = 1 Mohm` 到测试点 T，红笔接 T。
3. T 悬空，记录 `Vs` 和 `Vopen A(1 s/10 s/30 s)`；应约 `0.400-0.405 V`。
4. 关闭输出，把 T 只接 P16；确认 P16 是最左脚，P13 是左起第 4 脚。
5. 重新打开输出，记录 `P16 正、P13 负` 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性及异常。
6. 关闭输出，用约 `1 Mohm` 跨接 P16-P13 放电 30 s；随后拆下 A 相全部连接，再进行 B 相。

#### B 相：P13 正、P16 负

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P13
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P16
万用表 DCV 黑表笔 ───────────────────────────── OLED P16

OLED P1-P12、P14、P15：全部悬空
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

B 相顺序：

1. 确认 A 相已断电、放电并拆除；电源负极与黑表笔改接 P16。
2. 正极仍经唯一一只 `1 Mohm` 到 T，红笔接 T；T 先悬空，记录 `Vopen B`。
3. 关闭输出，把 T 只接 P13，再复核 P16/P13 位置无误。
4. 重新打开输出，记录 `P13 正、P16 负` 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性及异常。
5. 测完关闭输出，用约 `1 Mohm` 跨接 P13-P16 放电 30 s，拆除所有线并让 P1-P16 全部悬空。

任何一相出现 `CC`、接近 0 V 的强负载、持续下降、跳变、发热、异味或亮点，都立即断电并取消另一相。

结果判读：

- 两个方向都约等于各自 `Vopen`：P16 在该微安条件下为双向高阻，支持 `NC/未使用高阻脚`，但不能完全区分 NC 与高阻输入；
- P16 正、P13 负接近开路，而 P13 正、P16 负约 `0.27-0.30 V`：P16 具有与 P9/P10 相似的 VSS 保护结，是有效数字 PAD，否定 NC；
- 任一方向明显接近 0 V：停止，P16 可能与地或内部电源有强连接，不能继续换向。

返回格式：

```text
Vs =
Vopen A =
P16 正、P13 负：Vp 1 s / 10 s / 30 s =
CV/CC、稳定、异常 =

Vopen B =
P13 正、P16 负：Vp 1 s / 10 s / 30 s =
CV/CC、稳定、异常 =
```

## 2026-09-20：P16 物理断线确认与测试取消

用户指出 P16 从高清图上就是断开的，并补充断电测量：P16-P13 在电阻档和蜂鸣档均显示 `OL`。

重新放大核对 `/Users/liyao/Documents/github/OLED/pic/fpc1.jpg` 与 `fpc2.jpg` 后可见：

- P16 是最左侧真实金手指，但其下方铜箔只形成一段圆头短舌，在连接端加固区内终止；
- P15-P1 的铜线均继续向下延伸并进入扇出/驱动区，P16 没有同样的连续走线；
- 因此此前“16 条独立粗线全部进入驱动区”的照片描述有误，应修正为“16 个金手指中有 15 条有效下行走线，P16 为未布线触点”。

证据分级：

- 照片直接确认 P16 铜线物理终止：`CONFIRMED_STATIC`；
- P16-P13 电阻档/蜂鸣档 `OL` 与照片相符，但 `OL` 单独只能说明两脚没有低阻通路，不能单独证明 NC；
- 两项证据合并后，当前把 `P16=NC/未布线连接器触点` 作为强确认结论。

安全修正：

- 取消上一节计划的 P16-P13 双向微安测试；
- 不给 P16 供电、不接万用表带电测试点、不接 GPIO；
- P16 以后只作为机械编号和方向标记，不计入 OLED 的 15 条有效接口线；
- 本屏应按“16 位连接器、15 条有效排线、P16 NC”继续检索。

这项修正显著增强以下 15 条有效针序候选，但具体名称仍需正式资料闭环：

```text
P1-P4  = C2/C1 飞跨电容节点候选
P5     = VBAT 候选
P6     = VDD 候选
P7     = VCOMH/内部模拟节点候选
P8     = D2/SDAOUT 候选
P9     = D1/SDAIN 候选
P10    = D0/SCL 候选
P11    = D/C#/SA0 候选
P12    = RES# 候选
P13    = VSS 强候选
P14    = IREF 候选
P15    = VCC/VPP 候选
P16    = NC/未布线（照片确认）
```

### 当前安全状态与下一步

```text
实验电源正极/负极：全部断开
万用表表笔：移开 OLED
OLED P1-P16：全部悬空
ESP32-C3、USB、GPIO、所有旧电阻网络：全部断开
```

下一步先按“15 条有效线 + P16 NC”的特征重新检索正式针脚资料，不再进行 P16 电测。若资料仍无法闭环，再单独设计针对 P15/VCC 候选或 P1-P15 历史 `11.7 ohm` 矛盾的断电复核；未给出新接线图前不要自行测 P15。

## 2026-09-20：下一步确定为受控复核 P1-P15 历史低阻

当前最大的未解决矛盾是：候选针序把 P1 视为飞跨电容节点、P15 视为 VCC/VPP，两者应是不同节点；但历史记录曾得到 P1-P15 约 `11.7 ohm`。在处理这个矛盾前，不能给 P15 正常工作电压，也不应搭 P1-P4 电荷泵电容。

本轮不直接使用蜂鸣档或低阻档碰已插入的 OLED，而继续使用已校准的 `0.80 V + 1 Mohm` 夹具，把最大电流限制在约 `0.8 uA`。这里只判断“是否确有近短路”，不尝试命名 P1/P15。

### 引脚位置

```text
正视金手指朝上、丝印可读：

左端                                                                 右端
P16  P15  P14  P13  P12  P11  P10  P9  P8  P7  P6  P5  P4  P3  P2  P1
      ▲                                                               ▲
      │                                                               └── 电源负极 + 黑表笔
      └── 1 Mohm 后端 + 红表笔

P16：NC，完全不接
P15：左起第 2 脚
P1 ：最右侧脚
```

### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P15
                                                │
                                          测试点 T / Vp
                                                │
万用表 DCV 红表笔 ─────────────────────────────┘

实验电源负极 ───────────────────────────────── OLED P1
万用表 DCV 黑表笔 ───────────────────────────── OLED P1

OLED P2-P14、P16：全部悬空
特别注意：P13 本次不接地、不接表笔，必须悬空
ESP32-C3、USB、GPIO、3V3、旧 2.2k/10k/51k、200k：全部断开
```

### 操作顺序

1. 关闭实验电源、拔掉 USB，拆除全部旧测试线；若 P5-P13 仍可能有残压，用约 `1 Mohm` 跨接 P5-P13 放电 30 s 后拆除。
2. 电源负极和万用表黑笔只接 P1；电源正极经唯一一只 `1004 = 1 Mohm` 到测试点 T，红表笔接 T。
3. T 保持悬空、不碰 P15，打开输出，记录 `Vs` 和 `Vopen(1 s/10 s/30 s)`；正常应约 `0.400-0.405 V`。
4. 关闭输出；把 T 只接 P15，再核对 P15 是左起第 2 脚、P1 是最右脚，P16 仍悬空。
5. 打开输出，记录 P15 对 P1 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常情况。
6. 测完关闭输出；用约 `1 Mohm` 跨接 P15-P1 放电 30 s，再拆除所有线并让全部引脚悬空。

任何时候出现 `CC`、持续下降、跳变、发热、异味或亮点，都立即关闭输出。若 `Vp` 已接近 `0 V`，记录后立即断电，不交换极性，也不继续测 P15。

### 判读边界

- `Vp≈0-0.01 V` 且稳定：确认当前夹具下 P1-P15 存在近低阻路径；随后再单独区分转接板短路还是屏内低阻。当前候选针序必须暂停，P15 禁止供电。
- `Vp≈Vopen≈0.40 V`：历史 `11.7 ohm` 很可能来自旧接线、旧网络或测量条件，本轮没有复现低阻。
- `Vp` 落在中间或随时间变化：存在内部模拟/半导体路径，但不是简单 `11.7 ohm` 近短；不要自行换向，先提交读数。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
P15 对 P1：Vp 1 s =
P15 对 P1：Vp 10 s =
P15 对 P1：Vp 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-21：P15 正、P1 负的受控复核结果

- 用户报告：同轮 `Vopen = 0.4 V`；`P15 对 P1：Vp(1 s/10 s/30 s) = 0/0/0 V`。
- 原始接法沿用上一节：实验电源正极经唯一一只 `1 Mohm / 1004` 接 P15，电源负极接 P1；万用表红笔在 P15 测试点、黑笔在 P1；其余 OLED 引脚应全部悬空。
- 时间特征：三个时间点均为仪表显示的 `0 V`，没有观察到爬升或恢复。与 `Vopen = 0.4 V` 相比，这是明确且稳定的强导通，不是开路基准或末位波动。
- 判定标签：`CONFIRMED_STATIC`（在当前方向、约 `0.80 V + 1 Mohm` 夹具和仪表分辨率下，P15-P1 存在近低阻路径）；历史 `P1-P15 ≈ 11.7 ohm` 得到定性复现，但本轮不能确认精确阻值为 `11.7 ohm`。
- 能证明：该路径在最大约 `0.8 uA` 的受限偏置下足以把测试点压到仪表零位，故不能再把历史低阻简单归因于一次读数噪声。
- 不能证明：尚不能区分空转接板短路、夹线/针位问题、FPC/屏内真实低阻、保护结或电荷泵内部开关路径；也不能据此判定 OLED 损坏或推翻全部候选针序。
- 尚缺元数据：本轮未报告精确 `Vs`、`CV/CC`、整体稳定性、发热/异味/亮点等异常状态，以及空转接板 P1-P15 的隔离结果。

### 立即停止条件与下一单一动作

1. 立即关闭实验电源，不交换极性，不提高电压，不降低串阻，也不给 P15 正常工作电压。
2. 只用约 `1 Mohm` 跨接 P15-P1 放电约 30 s，随后拆除全部测试线并让 OLED 所有引脚悬空。
3. OLED 从转接板拔下后，先保持同一 `0.80 V + 1 Mohm` 夹具和同一极性，复测空转接板的 P15 对 P1；先确认同轮悬空 `Vopen` 仍约 `0.4 V`。空板若仍读 `0 V`，路径在夹线、面包板或转接板；空板若恢复约 `Vopen`，路径随 OLED 组件出现。
4. 只有 OLED 已拔下且转接板完全无源时，才可用电阻/蜂鸣档进一步定位：确认排针 P1-P15 之间为 `OL`，并分别确认 P1、P15 各自从 FPC 触点到对应排针导通、两路没有交叉。
5. 在提交空板结果以及本轮 `CV/CC/稳定性/异常` 信息前，不重新插入 OLED，不进行反向 P1-P15 测试，不恢复 ACK、I2C、GPIO 或电荷泵外围。

若空转接板 P1-P15 不是 `OL`，先处理转接板/焊接短路，本轮不能归因于 OLED；若空板隔离正常，则低阻位于 OLED/FPC/驱动内部，下一阶段才评估它是预期的电荷泵开关路径还是异常短路。

## 2026-09-21：空转接板 P1-P15 隔离复核

- 用户报告：`本轮 Vs = 0.4 V`；电源为 `CV`；稳定、无异常；空板 `Vopen = 0.4 V`；空板 `P15 正、P1 负 Vp = 0.4 V`；空板断电电阻档 `P1-P15 = OL`。
- 空板带电读数与同轮开路基准相同，且断电电阻档为 `OL`。两种方法互相支持：当前夹线和空转接板没有形成 P15-P1 导通路径。
- 与上一轮“插入 OLED 后 P15 正、P1 负为 `0/0/0 V`”组合后，可把强导通定位为“随 OLED 组件插入才出现”。它可能位于 FPC 接触/对位、COF/驱动芯片或玻璃内部，而不是空转接板自身。
- 该结果仍不能证明 OLED 损坏，也不能确认精确电阻。若 P1/P15 的候选确属电荷泵相关节点，内部 MOS 开关、体二极管、钳位或放电路径仍是可能解释；需要方向性和正式资料继续区分。
- 判定标签：`CONFIRMED_STATIC`（空转接板 P1-P15 隔离；强导通随 OLED 出现）；`UNCONFIRMED`（屏内路径性质和针脚名称）。

### 测点歧义：先重新确认 Vs

本轮填写的 `Vs = 0.4 V` 与此前夹具定义不一致：`Vs` 应直接测实验电源正负输出端，历史值约 `0.80-0.81 V`；`0.4 V` 通常是 1 Mohm 后端悬空测试点的 `Vopen`。在澄清前不进行反向 P1-P15 测试。

下一单一动作只校准夹具，不接 OLED，也不接空转接板：

```text
A. 直接测电源输出 Vs

实验电源正极 ───────── 万用表红笔
实验电源负极 ───────── 万用表黑笔

OLED、转接板、1 Mohm 后端：全部不接

B. 再测开路基准 Vopen

实验电源正极 ── 1 Mohm / 1004 ──● T（悬空，不接任何板）
                                      └── 万用表红笔
实验电源负极 ─────────────────────── 万用表黑笔

OLED、转接板：全部不接
```

先关输出完成 A 相接线，开输出记录 `Vs`；再关输出改为 B 相，开输出记录 `Vopen`。预期若仍是原夹具，应为 `Vs≈0.80 V`、`Vopen≈0.40 V`。只返回这两个直接读数；确认后才决定是否用同一微安级条件做 P1 正、P15 负的反向指纹。

## 2026-09-21：空转接板复核数据更正

- 用户更正上一节的测点名称：`Vs = 0.8 V`，不是 `0.4 V`；其余结果不变：`CV`、稳定、无异常，空板 `Vopen = 0.4 V`，空板 `P15 正、P1 负 Vp = 0.4 V`，空板断电电阻档 `P1-P15 = OL`。
- 更正后的 `Vs/Vopen = 0.8/0.4 V` 与已校准的 `1 Mohm` 串阻、约 `1 Mohm` 万用表输入阻抗完全相符；上一节的测点歧义已经消除，不需要重复夹具校准。
- 结论升级：夹具和空转接板在带电微安测试与断电电阻档下均未形成 P15-P1 通路；插入 OLED 后才出现的 `Vp = 0 V` 可定位到 OLED 组件或“插入后才发生”的连接器接触/对位状态。
- 仍不能区分：正常的屏内电荷泵/保护开关路径、FPC 插入造成的跨脚接触、COF/驱动内部异常；也不能据此认定 OLED 损坏。
- 当前继续禁止 P15 工作电压、降低串阻、ACK/I2C/GPIO 和电荷泵外围。下一步只考虑在同一 `0.80 V + 1 Mohm` 限流条件下做反向 `P1 正、P15 负` 指纹；正式接线图和停机条件需先完成复核。

### 下一单一实验：同一次插接下的 P15-P1 双向微安指纹

目的：先在重新插入 OLED 后复现已知方向，再在完全断电、放电后交换极性。这样可以区分插接状态变化与稳定的屏内方向性路径。两相不得在线换线；若 A 相不再复现 `0 V`，本轮立即停止，不做 B 相。

前置条件：OLED、实验电源、ESP32 和 USB 均断开；目检 FPC 金手指和连接器没有歪插、跨脚、异物或污染；按已确认方向把 OLED 平直插入转接板。P2-P14、P16 及全部旧电阻/GPIO 均悬空，尤其 P13 不接地。

#### A 相：复现 P15 正、P1 负

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P15
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── OLED P1
万用表黑表笔 ───────────────────────────────── OLED P1

OLED P2-P14、P16：全部悬空
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

1. T 先悬空，记录 `Vs` 和 `Vopen A`；应约为 `0.80 V / 0.40 V`。
2. 关闭输出，把 T 只接 P15；打开输出并记录 `Vp(1 s/10 s/30 s)`、`CV/CC` 和异常。
3. 若 A 相不再为约 `0 V`，立即结束本轮并记录为插接/接触状态相关，不进行 B 相。
4. 若 A 相稳定复现约 `0 V` 且无异常，关闭输出，用约 `1 Mohm` 跨接 P15-P1 放电 30 s，再拆除 A 相连接。

#### B 相：P1 正、P15 负

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── OLED P1
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── OLED P15
万用表黑表笔 ───────────────────────────────── OLED P15

OLED P2-P14、P16：全部悬空
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

1. T 先悬空，记录 `Vopen B`；关闭输出后把 T 只接 P1。
2. 打开输出，记录 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常。
3. 测完关闭输出，用约 `1 Mohm` 跨接 P1-P15 放电 30 s，拆除全部连接并让所有引脚悬空。

任何一相出现 `CC`、跳变、持续下降、发热、异味或亮点，都立即关闭输出并取消余下步骤；不使用电阻、蜂鸣或二极管档测已插入的 OLED。

判读：

- A 相约 `0 V`、B 相约等于 `Vopen`：强方向性，支持内部半导体/开关路径，反对简单双向欧姆短路；
- A、B 两相都约 `0 V`：支持双向低阻、闭合开关或物理短接，但仍不能认定损坏或确认精确 `11.7 ohm`；
- 任一相为中间值或随时间变化：属于非线性/电容性路径，停止命名并复核原始数据。

返回格式：

```text
Vs =
Vopen A =
A：P15 正、P1 负，Vp 1 s / 10 s / 30 s =
A：CV/CC、稳定、异常 =

Vopen B =
B：P1 正、P15 负，Vp 1 s / 10 s / 30 s =
B：CV/CC、稳定、异常 =
```

## 2026-09-21：15 针精简 SPI 公版候选重新进入优先级

在 P15-P1 低阻确认随 OLED 出现后，重新检索到两份不同尺寸、不同控制器但使用相同 15 针顺序的正式面板规格：

1. `ZJY109-6428TSWEG04`：1.09 英寸、64x128、SSD1312、15 针、0.65 mm；规格书：<https://www.digole.com/products_pictures/YWRtaW4%3D/42/6428TSWEG04%20.pdf>。
2. `WED0244XXAWPP3N00000`：0.91 英寸、128x32、SSD1306、15 针、0.65 mm；规格书：<https://www.winstar.com.tw/d/635/WED0244XXAWPP3N00000.pdf>。

两者共同的针序为：

```text
P1  C2P       P6  VSS       P11 SCLK
P2  C2N       P7  VDD       P12 SDIN
P3  C1P       P8  CS#       P13 IREF
P4  C1N       P9  RES#      P14 VCOMH
P5  VBAT      P10 D/C#      P15 VCC/VPP
```

本屏并非上述任一公开型号：实物是 `0.5 mm / 16 位连接器 / P16 未布线`，丝印也不同。因此不能直接套用它们的电压、外围和初始化。但同一针序跨 SSD1306/SSD1312 两种面板重复出现，说明它是成熟的“精简 4 线 SPI + 内部电荷泵”公版；本屏可能是在 16 位连接器上增加 P16 机械空脚的变体。

该候选与实物照片的几何分组高度一致：

```text
P15-P13 = VCC / VCOMH / IREF 模拟组
P12-P8  = SDIN / SCLK / D-C# / RES# / CS# 数字组
P7-P6   = VDD / VSS 逻辑电源组
P5-P1   = VBAT + 两组飞跨电容组
P16     = NC
```

它也提供了一个能解释旧结果的竞争模型：旧 `0x3C` 测试可能实际把 4 线 SPI 的 `CS#/RES#/D-C#/SCLK/SDIN` 当成 I2C 线，因此 NACK 不具地址判别意义；此前 P9/P10 相对 P13 的方向性也可能是数字输入相对 IREF 模拟节点的内部路径，而不必要求 `P13=VSS`。

证据等级调整：

- 上述 15 针 SPI 顺序升级为新的 `STRONG_HYPOTHESIS`，但未达到本屏资料闭环；
- `P13=VSS`、P8-P12 为分离 SDA/I2C 组降回 `UNCONFIRMED`；
- `P1=C2P` 与 `P15=VCC/VPP` 在两份公版中一致，且两者同属内部开关电荷泵网络；这使 P15-P1 导通具有拓扑合理性，但规格书没有给出断电等效电路、导通方向或阻值，不能把 `Vp=0` 直接判为正常或异常；
- 在双向微安指纹和本屏正式资料闭环前，仍禁止正常供电、搭飞跨电容或尝试 SPI 初始化。

## 2026-09-21：P15-P1 双向微安指纹结果

- 共同条件：`Vs = 0.8 V`；两相 `Vopen = 0.4 V`；唯一串阻为 `1 Mohm / 1004`；电源均为 `CV`、稳定、无异常。
- A 相 `P15 正、P1 负`：`Vp(1 s/10 s/30 s) = 0/0/0 V`。
- B 相 `P1 正、P15 负`：`Vp(1 s/10 s/30 s) = 0/0/0 V`。
- 两个方向在三个时间点都压到仪表零位，证明这是稳定的双向强导通，不符合“只有一个普通单向保护二极管”的简单模型，也不像孤立飞跨电容的一次充电瞬态。
- 结合空转接板 `Vp=Vopen=0.4 V`、断电 `P1-P15=OL`，该双向通路随 OLED/FPC 插入出现；历史约 `11.7 ohm` 得到第二种夹具下的定性复现，但本轮仍不能测出精确阻值。
- 可能解释仍包括：电荷泵内部保持闭合的 MOS 开关/放电结构、COF 或玻璃内部设计性低阻、插入后才产生的连接器跨脚接触，或器件内部异常短路。现有公开规格没有规定断电时 C2P-VCC 应呈现的电阻，因此不能据此确认正常或损坏。
- 判定标签：`CONFIRMED_STATIC`（P15-P1 在当前微安条件下为双向强导通）；`UNCONFIRMED`（路径性质、控制器型号和器件健康状态）。
- 安全收尾：关闭输出，用约 `1 Mohm` 跨接 P1-P15 放电约 30 s，拆除全部连接并让所有引脚悬空。停止继续测 P1/P15，不降低串阻、不提高电压、不给 P15 工作电压。

## 2026-09-21：下一步改用第二片同型号裸屏作对照

第一片的 P1-P15 已确认在两个方向均把测试点压到 `0 V`，但仅凭同一片样品无法区分“该型号固有的内部开关/放电结构”和“第一片个体损坏或插接异常”。因此暂停扩大第一片的针脚测试矩阵；若第二片是同型号、同 FPC 标识且此前未上电，优先把它作为独立对照。

### 测试前身份与状态检查

- 两片屏保持断电，分别标为“样片 1（已测）”和“样片 2（对照）”，不要混放。
- 目检样片 2 与样片 1 的外形、FPC 丝印（当前记录为 `30033 / A0201001`）、16 个触点、触点编号方向及 P16 铜箔终止形态是否一致。
- 若型号/丝印、针数或走线外观不一致，样片 2 不能当作直接对照，本测试停止。
- 样片 2 不做电阻档、蜂鸣档或二极管档测试；仍只用 `Vs≈0.80 V + 唯一一只 1 Mohm / 1004` 的微安夹具。

### A 相：样片 2 的 P15 正、P1 负

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── 样片 2 P15
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── 样片 2 P1
万用表黑表笔 ───────────────────────────────── 样片 2 P1

样片 2 P2-P14、P16：全部悬空
样片 1：完全断开并单独放置
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

1. 不插屏时先确认 `Vs≈0.80 V`；1 Mohm 后端 T 悬空，记录 `Vopen A`，应约为 `0.40 V`。
2. 关闭输出，平直插入样片 2；把 T 只接 P15，负极与黑表笔只接 P1。
3. 打开输出，记录 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常。
4. 关闭输出，用约 `1 Mohm` 跨接 P15-P1 放电 30 s，再拆除 A 相连接。禁止在线换极性。

### B 相：样片 2 的 P1 正、P15 负

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── 样片 2 P1
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── 样片 2 P15
万用表黑表笔 ───────────────────────────────── 样片 2 P15

样片 2 P2-P14、P16：全部悬空
样片 1：完全断开并单独放置
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

1. T 再次悬空，记录 `Vopen B`；关闭输出后把 T 只接样片 2 的 P1。
2. 打开输出，记录 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常。
3. 测完关闭输出，用约 `1 Mohm` 跨接 P1-P15 放电 30 s，拆除全部连接并让样片 2 所有引脚悬空。

任一相出现 `CC`、跳变、持续下降、发热、异味或亮点，立即关闭输出并取消余下步骤。不要降低串阻、提高电压、搭飞跨电容或尝试正常供电/协议。

### 对照判读边界

- 样片 2 也为双向 `0/0/0 V`：大幅支持这是同型号裸屏共有的内部低阻/开关结构，而不是样片 1 的偶发损坏；但使用同一个连接器时仍不能完全排除“插入后机械跨脚”的共同因素，也仍不能确认精确阻值或具体针名。
- 样片 2 双向都接近 `Vopen`：样片 1 属于个体异常或其插接状态异常的可能性显著升高；停止用样片 1 继续反推针序。
- 样片 2 只有一个方向明显降低：说明存在方向性半导体路径，样片 1 的双向强导通不是该状态的正常复现；停止继续上电，先复查两片外观、插接和来源一致性。
- 任一结果不稳定或两片身份不能确认：结果不用于针序判断，优先索取原厂 drawing/pin table。

返回格式：

```text
样片 2 是否同丝印/同外观、此前是否未上电 =
Vs =
Vopen A =
A：样片 2 P15 正、P1 负，Vp 1 s / 10 s / 30 s =
A：CV/CC、稳定、异常 =

Vopen B =
B：样片 2 P1 正、P15 负，Vp 1 s / 10 s / 30 s =
B：CV/CC、稳定、异常 =
```

## 2026-09-21：第二片裸屏 P1-P15 对照结果

- 按上一节“第二片对照”的连续语境，以下读数暂记为样片 2；用户本次未另外填写“同丝印/同外观、此前是否未上电”，因此这项样品身份元数据仍待补充，但不改变原始电测记录。
- 共同条件：`Vs = 0.8 V`；两相 `Vopen = 0.4 V`；电源均为 `CV`、稳定、无异常。
- A 相，样片 2 `P15 正、P1 负`：`Vp(1 s/10 s/30 s) = 0/0/0 V`。
- B 相，样片 2 `P1 正、P15 负`：`Vp(1 s/10 s/30 s) = 0/0/0 V`。
- 该结果完整复现样片 1：两片裸屏在相同微安夹具下，P1-P15 均为稳定的双向强导通。若两片确为同型号且样片 2 此前未受未知上电，则“样片 1 偶发内部损坏”已明显降级，优先解释转为该型号共有的内部开关/放电结构或其他设计性通路。
- 两片都使用同一个转接板/连接器，故严格说仍共享“插入后接触或对位形成通路”这个因素；空板自身虽已验证为开路，但不能仅凭当前数据完全排除插屏后才发生的机械伪像。不过 P1 与 P15 非相邻脚，且两片行为完全一致，这个解释的优先级低于共有内部结构。
- 两片共同读数仍不能确认精确阻值、控制器型号或针名，也不能证明 P1-P15 在正常上电状态下应短接。公开近似规格未给出完全断电时 C2P-VCC/VPP 的等效电路。
- 证据标签：`REPRODUCED_ON_SECOND_SAMPLE`（第二片复现）；`STRONG_HYPOTHESIS`（P1-P15 双向低阻为该型号共有特性）；`UNCONFIRMED`（样片 2 身份元数据、通路具体性质与完整针序）。
- 样片 2 测完后关闭输出，用约 `1 Mohm` 跨接 P1-P15 放电 30 s，并将所有引脚悬空。后续针序判别优先只使用样片 1，保留样片 2 作为对照，不再扩大其测试范围。

### 下一单一动作：仅在样片 1 补测 P9 正、P6 负

目的：历史同类夹具曾测得样片 1 `P6 正、P9 负 = 0.322 V`，但反方向尚未测量。当前两套竞争模型都把 P9 视为数字输入，却分别把 P6 解释为不同电源轨：

```text
旧候选：P6 = VDD，P9 = 数字输入
新 15 针 SPI 候选：P6 = VSS，P9 = RES# 输入
```

因此只补 `P9 正、P6 负`，观察数字输入相对 P6 的保护/偏置方向。该实验只提供候选模型的相对支持，不能单独确认针名。

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── 样片 1 P9
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── 样片 1 P6
万用表黑表笔 ───────────────────────────────── 样片 1 P6

样片 1 P1-P5、P7-P8、P10-P16：全部悬空
样片 2：放电后全脚悬空，作为留样单独保存
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

#### 操作顺序

1. 确认样片 2 已完全断开；关闭输出，用约 `1 Mohm` 对此前测试节点放电约 30 s。
2. 只将样片 1 平直插入；检查 P9 是左起第 8 脚、P6 是左起第 11 脚，其余脚没有接线。
3. 测试点 T 先悬空；记录 `Vs` 及 `Vopen(1 s/10 s/30 s)`。在当前夹具下应约为 `0.80 V / 0.40 V`。
4. 关闭输出，把 T 只接样片 1 的 P9；电源负极和黑表笔只接 P6。
5. 打开输出，记录 `P9 正、P6 负` 的 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常。
6. 测完先关闭输出，用约 `1 Mohm` 跨接 P9-P6 放电 30 s，拆除全部连接并让样片 1 所有引脚悬空。

出现 `CC`、跳变、持续下降、发热、异味或亮点时立即关闭输出；不降低串阻、不提高电压、不在线换线，也不使用电阻、蜂鸣或二极管档。

#### 预先固定的判读边界

以同轮 `Vopen≈0.400 V` 为基准：

- `Vp` 在三个时间点均约 `0.38-0.40 V`：与历史反向 `0.322 V` 形成明确方向性，较支持新 SPI 候选的 `P6=VSS、P9=输入`，并削弱旧 `P6=VDD`；仍不能单独确认针名。
- `Vp` 稳定约 `0.30 V` 或更低：说明 P9 到 P6 存在较强结/偏置路径，较相容旧候选的 `P6=VDD` 上侧保护钳位；新候选下的特殊复位偏置或经浮空 VDD 的路径仍不能完全排除。
- `Vp≈0.31-0.37 V`：灰区，只记录为弱钳位，不能据此选择针序。
- 初始较低、随后逐渐回到 `Vopen`：优先视为浮空电源轨充电或电容效应，以 30 s 读数记录，不直接命名。
- `Vp≈0 V`、明显跳变、`CC` 或任何异常：立即停止；不再扩大样片 1 的电测矩阵。

返回格式：

```text
补充确认：样片 2 同丝印/同外观、此前未上电 =
Vs =
Vopen 1 s / 10 s / 30 s =
样片 1 P9 正、P6 负：Vp 1 s / 10 s / 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-21：背面基础料号与三片样品身份补充

用户补充目前共有三片裸屏，并确认前两片背面标记分别为：

```text
样片 1（此前主要测试片）：100655001P D7
样片 2（P1-P15 对照片）：100655001P N4
样片 3：完整后缀及既往上电状态待记录
```

- `100655001P` 在两片上完全相同，且仓库原始商品图的整盘屏幕背面标签中也能看到同一基础串，因此它是当前最有价值的供应链料号/面板家族检索键，不像单片随机测量编号。
- `D7`、`N4` 位于共同基础串之后，更像批次、生产线、分档、日期/位置或版本后缀；没有原厂编码规则，当前不能擅自解释其含义，也不能仅凭后缀断言两片电气版本完全相同。
- 两个不同后缀的样片仍完整复现 P1-P15 双向 `0 V`，进一步削弱了“只发生在单个后缀/单片上的偶发故障”，并支持该通路属于 `100655001P` 家族的共有特征；仍不能把它直接等同于“正常工作状态”。
- 公开网页按 `100655001P`、`100655001P D7`、`100655001P N4` 及 OLED/FPC/显示屏组合检索均未得到可信精确匹配。该串可能是未公开的 OEM/供应链内部料号，最适合用于向原卖家、拆机设备厂或面板供应商索取原始 drawing、pin table、驱动 IC 和上电时序。
- 样片 3 暂不插转接板、不接万用表，也不做任何电阻/蜂鸣/二极管或微安测试；先拍清晰背面全景与料号近照，记录完整后缀，并确认此前是否从未上电。它应作为当前唯一的未测试留样保存。
- 在样片 3 身份记录完成前，上一节的样片 1 `P9 正、P6 负` 计划先暂停，不取消。

### 第三片标记确认：R 版本未上电留样

用户补充：

```text
样片 3 完整背面标记（按用户原样转写）：100655001R O 4
此前是否上电：未上电
```

- 样片 3 的基础串末字母为 `R`，不同于样片 1/2 的 `P`；因此当前按 `100655001R` 的另一变体管理，不能把它当作 `100655001P` 的完全同型号盲样，也不用于复测 P1-P15。
- `O 4` 暂按用户可见字符原样保留；在正对焦照片确认前，不擅自归一化为字母 `O4` 或数字 `04`。同样需要由照片复核末字母确为 `R` 而非点阵字体中的相似字符。
- 对 `100655001R O4`、`100655001R 04`、`100655001RO4`、`100655001R` 与 OLED/FPC/显示屏组合进行公开检索，未找到可信精确匹配或原厂规格。
- 样片 3 是目前唯一明确“从未上电”的留样。继续保持不插转接板、不接任何仪表、全脚悬空并作 ESD 防护保存。
- 当前下一单一动作改为纯视觉记录：在断电状态拍摄三片背面料号并排微距照片，确保 `P/R`、`O/0`、空格及后缀清楚可辨；完成前不进行新的电测。样片 1 的 `P9 正、P6 负` 计划继续保留但暂缓。

### `pic/3.jpg` 三片并排照片复核结果

已检查 `/Users/liyao/Documents/github/OLED/pic/3.jpg` 的原图并分别放大三个点阵编号区域。按照片从上到下可读为：

```text
上：100655001P D7
中：100655001R 04
下：100655001P N4
```

- 中间样片的后两位第一字符与同一行内数字 `0` 的点阵形态一致，应记录为数字 `04`，不是字母 `O4`。用户此前的 `O 4` 转写据照片更正为 `04`。
- 三片最稳妥的共同主干是九位数字 `100655001`；其后的 `P/R` 也可能属于修订、工艺、颜色、批次或其他追溯字段，而不一定是基础料号本体的一部分。没有编码规则，不能解释 `P/R` 的具体意义。
- 照片中三片外形、16 位 FPC 和扇出布局高度相似；但中间 `R 04` 样片的 COF/封胶边缘外观比上下两片更粗糙、残留物更多。照片不能判定损坏，但它不适合作为比两片 P 编码更干净的电气对照。
- 样片对应关系据此固定为：样片 1=`P D7`，样片 2=`P N4`，样片 3=`R 04`（用户确认未上电）。样片 3 继续全脚悬空并作 ESD 留样，不复测 P1-P15。
- 纯视觉身份核对已经完成。若继续微安针序判别，只恢复样片 1 的 `P9 正、P6 负` 计划；样片 2/3 均不再扩大测试范围。精确原厂 drawing/pin table 仍优先于任何新增电测。

## 2026-09-21：样片 1 的 P9 正、P6 负微安结果

- 用户报告：`Vopen = 0.4 V`；样片 1 `P9 正、P6 负` 的 `Vp(1 s/10 s/30 s) = 0.4/0.4/0.4 V`。
- 三个时间点均与同轮开路基准相同，没有观察到可辨别的直流负载或充电爬升。
- 历史相反方向为样片 1 `P6 正、P9 负 = 0.322 V`（`Vopen=0.405 V`，1 s 与 30 s 相同；当时缺少 10 s 和完整状态元数据）。两方向组合为稳定不对称：

```text
P6 正、P9 负：0.322 V，存在弱钳位/负载
P9 正、P6 负：0.400 V，等同开路
```

- 该方向与数字输入的下侧保护路径相容：若新 15 针 SPI 公版候选正确，P6 为 VSS、P9 为 RES# 输入，则只有 `P6 正、P9 负` 方向出现钳位是合理的相对模式；若旧候选 `P6=VDD、P9=数字输入`，通常更预期 `P9 正、P6 负` 出现上侧保护钳位，本次开路结果明显削弱该简单模型。
- 判定升级：`STRONG_HYPOTHESIS`（P6=VSS、P9=数字输入，且 15 针精简 4 线 SPI 公版顺序优先于旧 P13=VSS/I2C 候选）；仍不是 `CONFIRMED`，因为未知芯片在完全掉电时可能有浮空电源轨、特殊复位偏置或非典型 ESD 网络。
- 用户随后补充：`Vs = 0.8 V`、`CV`、稳定、无异常。本轮元数据完整；`P9 正、P6 负` 在当前微安夹具下可记为 `CONFIRMED_STATIC` 高阻表现。
- 安全收尾：关闭输出，用约 `1 Mohm` 跨接 P9-P6 放电 30 s，拆除样片 1 的全部连接；样片 2/3 继续全脚悬空。

### 下一单一动作：同一夹具复测样片 1 的 P6 正、P9 负

目的：历史反方向虽记录为 `0.322 V`，但缺 `10 s`、精确 `Vs`、`CV/CC` 和异常状态。现在只复测这一方向，用完整元数据与刚完成的 `P9 正、P6 负 = 0.400 V` 配对；不扩大到其他针脚。

#### 完整接线图

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── 样片 1 P6
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── 样片 1 P9
万用表黑表笔 ───────────────────────────────── 样片 1 P9

样片 1 P1-P5、P7-P8、P10-P16：全部悬空
样片 2、样片 3：完全断开并作为留样保存
ESP32-C3、USB、GPIO、3V3、旧电阻网络：全部断开
```

1. 先关闭输出，用约 `1 Mohm` 跨接上一相的 P9-P6 放电 30 s，拆除上一相连线。
2. T 保持悬空，打开输出并记录 `Vs`、`Vopen(1 s/10 s/30 s)`；随后关闭输出。
3. 正极经唯一一只 `1 Mohm / 1004` 接样片 1 P6；负极和黑表笔只接 P9；红表笔接 P6 端测试点。
4. 再次确认 P6 是左起第 11 脚、P9 是左起第 8 脚，其余引脚全部悬空，然后打开输出记录 `Vp(1 s/10 s/30 s)`。
5. 测完关闭输出，用约 `1 Mohm` 跨 P6-P9 放电 30 s，拆除全部连接并全脚悬空。

出现 `CC`、跳变、持续下降、发热、异味或亮点时立即关闭输出。禁止在线换线、降低串阻、提高电压或测试样片 2/3。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
样片 1 P6 正、P9 负：Vp 1 s / 10 s / 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-21：样片 1 的 P6 正、P9 负反向复测结果

- 共同条件：`Vs = 0.8 V`；同一 `1 Mohm / 1004` 串阻；同一万用表和 P9 负端参考；`Vopen(1 s/10 s/30 s) = 0.4/0.4/0.4 V`。
- 原始读数：样片 1 `P6 正、P9 负` 的 `Vp(1 s/10 s/30 s) = 0.32/0.32/0.32 V`；`CV/CC = CV`；稳定；无异常。
- 该结果复现了此前不完整记录的约 `0.322 V`，并与同一夹具下的反向结果形成稳定不对称：

```text
P6 正、P9 负：0.32 V，稳定低于 Vopen
P9 正、P6 负：0.40 V，接近 Vopen
```

- 判定标签：`CONFIRMED_STATIC`（在微安限流条件下，P6-P9 存在可重复的方向性负载）；`STRONG_HYPOTHESIS` 仅用于相对关系（P9 相对 P6 呈现受保护数字输入/下侧结样式的路径）。这与“P6 为较低电位/参考候选、P9 为数字输入候选”相容，也与“P6 为另一电源/模拟节点、经内部保护网络连接到 P9”相容，不能单独选择两种模型。
- 夹具等效估算：若沿用串阻约 `1 Mohm`、万用表输入约 `1 Mohm`，`Vp=0.32 V` 对应总负载约 `0.67 Mohm`；扣除万用表支路后，样片附加支路约为 `2 Mohm`、工作点电流约 `0.16 uA`。这只是该微安工作点的小信号等效，不是器件真实电阻或芯片规格。
- 与前文交叉核对：此前 `P13 正、P9 负` 和 `P13 正、P10 负` 均约 `0.28 V`、反向约 `0.40 V`，独立支持 P13 为公共低参考候选。因此本次不能把 P6 命名为 VSS，也不能据此否定 P13=VSS；P6/P13 谁是 VSS、P9 的具体功能仍未决。15 针精简 SPI 针序只是兼容候选，不是本次确认。
- 这次结果可以有限削弱“P6=VDD、P9=输入且应出现相反方向上侧钳位”的简单模型，但掉电浮空电源轨、保护结、内部偏置或接触因素仍可能造成同样读数。
- 不能据此确认：P6 的确切名称（VSS/VDD 等）、P9 的确切名称（RES#/其他数字脚）、控制器型号或正常工作电源接法。
- 安全结论：本轮没有 CC、掉压、发热、异味或其他异常；该结果不是正常供电测试，不能据此降低串阻、提高电压或给 P6/P15 直接供电。
- 安全收尾：关闭实验电源，用约 `1 Mohm` 跨接 P6-P9 放电约 30 s，拆除样片 1 全部连接；样片 2、样片 3 保持断开，样片 3 继续作为未上电留样。

### 当前决策

P6/P9 的方向性配对已完成，不再重复该组合或扩大微安注入矩阵。当前应保持全部引脚悬空，优先寻找与 `100655001P`、15 条有效线、P16 未布线、0.5 mm FPC 相匹配的正式 pin table、驱动 IC 和上电时序。没有资料闭环前，不恢复旧的 I2C/ACK/U8g2 固件，不搭 P1-P4 飞跨电容，也不对 P7/P15 施加正常工作电压。

## 2026-09-21：商家无法提供资料后的路线调整

- 用户反馈：商家无法提供该屏的原始 drawing、pin table、驱动 IC 或上电时序。
- 决策：不再等待商家资料，也不继续随机扩大微安注入矩阵；三片样品保持断电，样片 3 继续作为未上电留样。
- 首选替代证据是寻找同源完整模块、带 FPC 座的测试板、拆机主板或同批次可点亮组件。检索键保留：`100655001P`、`100655001P D7/N4`、`30033`、`A0201001`、`0.5 mm`、`16P`、约 `1.1 inch / 128x64`。同外观件在未核对 FPC 走线前不能假定针序相同。
- 若找不到同源板，再购买低成本 USB 显微镜或体视显微镜做断电、无损的 FPC 走线、P1/P16、COF 边缘和激光字记录；不要刮胶、撬 COF 或用探针碰封胶下的键合线。显微镜只能改善几何证据，不能单独命名协议。
- 逻辑分析仪和示波器暂缓：裸屏没有主机波形可抓，先买仪器不会解决针序问题。X 光、CT、脱胶属于最后手段，不动样片 3。
- 在同源板暂时无法取得的情况下，只追加一次尚未完成的 `P13 正、P6 负` 微安反向测试，与历史 `P6 正、P13 负 = 0.224/0.224/0.225 V` 配对。该测试完成后停止扩大矩阵；任何结果都不能直接授权 3.3 V、飞跨电容或协议初始化。

### 下一单一动作：样片 1 的 P13 正、P6 负反向微安测试

目的：只比较 P6-P13 两个方向的掉电微安指纹，有限地区分“P6 是较低参考、P13 是 IREF/输入类节点”和“P13 是较低参考、P6 是电源/模拟轨”两个竞争模型。未知芯片内部网络可能让两种模型产生重叠结果，因此这不是针名确认测试。

```text
实验电源正极（约 0.80 V） ── 1 Mohm / 1004 ──●── 样片 1 P13
                                                └── 万用表红笔
实验电源负极 ───────────────────────────────── 样片 1 P6
万用表黑表笔 ───────────────────────────────── 样片 1 P6

样片 1 P1-P5、P7-P12、P14-P16：全部悬空
样片 2、样片 3：完全断开；样片 3 保持未上电留样
ESP32-C3、USB、GPIO、3V3、220k 和全部旧电阻网络：全部断开
```

1. 关闭输出，用约 `1 Mohm` 跨接上一轮 P6-P9 约 30 s，随后拆除上一轮全部接线。
2. 测试点 T 保持悬空，打开输出确认 `Vs` 和 `Vopen(1 s/10 s/30 s)`；预期约为 `0.80 V / 0.40 V`，否则停止检查夹具。
3. 关闭输出。正极经唯一一只 `1 Mohm / 1004` 接 P13，负极和黑表笔只接 P6，红表笔接 P13 端测试点。按既定方向复核 P13 是左起第 4 脚、P6 是左起第 11 脚。
4. 打开输出，记录 `Vp(1 s/10 s/30 s)`、`CV/CC`、稳定性和异常。
5. 测完关闭输出，用约 `1 Mohm` 跨 P13-P6 放电约 30 s，拆除全部连接并全脚悬空。

预先固定的判读边界：

- `Vp = 0.38-0.40 V` 且三点稳定：该方向接近开路，较相容 `P6=低参考、P13=IREF/输入类节点`，但不确认针名。
- `Vp <= 0.30 V` 且三点稳定：P6-P13 两个方向都有明显内部路径，较相容 `P13=低参考、P6=电源/模拟轨`，仍不能确认针名。
- `Vp = 0.31-0.37 V` 或随时间变化：模型重叠，记为不可区分并停止。
- 接近 `0 V`、跳变、持续下降、进入 `CC`、发热、异味或亮点：立即关闭输出，不继续。

禁止改用 `220k`、提高电压、重测 P7/P15，或在本测试后直接进入 SPI/I2C/U8g2。

返回格式：

```text
Vs =
Vopen 1 s / 10 s / 30 s =
样片 1 P13 正、P6 负：Vp 1 s / 10 s / 30 s =
CV/CC =
是否稳定 =
是否异常 =
```

## 2026-09-21：样片 1 的 P13 正、P6 负反向微安结果

- 用户报告：`Vs = 0.81 V`；`Vopen(1 s/10 s/30 s) = 0.4/0.4/0.4 V`；样片 1 `P13 正、P6 负` 的 `Vp(1 s/10 s/30 s) = 0.29/0.29/0.29 V`；`CV/CC = CV`；稳定；无异常。
- 与已完成的相反方向配对：

```text
P6 正、P13 负：0.224/0.224/0.225 V（历史完整轮次）
P13 正、P6 负：0.29/0.29/0.29 V（本轮）
开路基准：约 0.40 V
```

- 两个方向都稳定低于 `Vopen`，但强度不同；这证明 P6-P13 存在双向的非线性/弱负载路径，不像单个理想单向二极管，也不是一次性电容充电。它仍可能由多个保护结、浮空电源轨、内部偏置或电荷泵开关共同造成。
- 夹具量级估算：若万用表输入约 `1 Mohm`，本轮 `Vp=0.29 V` 对应总负载约 `0.56 Mohm`，扣除表内支路后样片附加支路约 `1.3 Mohm`、工作点电流约 `0.23 uA`。这是当前偏置点的等效值，不是器件真实电阻。
- 判定标签：`CONFIRMED_STATIC`（P6-P13 双向结果均可复现且稳定）；`STRONG_HYPOTHESIS` 只保留给“P13 作为公共低参考、P6 作为电源/模拟轨”的整体解释；`UNCONFIRMED`（P6/P13 的确切针名、控制器和接口仍未确认）。本轮两方向都低于 `Vopen`，本身不能按单向边界选择模型。
- 结合此前 `P13 正、P9/P10 负 ≈0.28 V`、反向接近 `Vopen` 的独立结果，P13 仍是公共低参考的领先电气假设，但本轮只是没有推翻它，并未形成芯片级确认；双向 P6-P13 路径仍可能来自 15 针 SPI 候选中的 IREF/保护网络、浮空电源轨或内部开关。
- 安全收尾：关闭输出，用约 `1 Mohm` 跨接 P13-P6 放电约 30 s，拆除样片 1 全部连接；样片 2、样片 3 保持断开，样片 3 继续作为未上电留样。

### 微安级配对测试结束

P6/P13 这组反向配对已完成；不再降低串阻、提高电压或扩大逐脚注入矩阵。下一步转向寻找同源完整模块/测试板，或用低成本显微镜做断电 FPC 走线和插接对位检查；在取得外部证据前，不恢复 3.3 V、SPI/I2C、ACK、U8g2、飞跨电容或主动 GPIO。

## 2026-09-23：WMV 显微视频复核结果

用户提供的视频 `/Users/liyao/Desktop/pic/20260923-193005-374.wmv` 已用 FFmpeg 解码并逐段抽帧复核。文件为 `WMV2`、`640x480`、约 `68.188 s`。抽帧目录仅作为本轮临时分析证据：`/tmp/oled-video-frames-20260923/`、`/tmp/oled-video-key-20260923/`、`/tmp/oled-video-detail-20260923/`。

视频不是一次从连接器到黑胶边缘的单向完整扫描，而是多次局部往返扫拍。可辨认的时间段包括：`0-3 s` COG/玻璃扇出细线；`3-5 s` FPC 标记区（可见 `30033`、`A0201001` 和 `16`）；`4-12 s` 连接器触点、FPC 主干和弯折区；`13-20 s` 黑胶/玻璃扇出；`20 s` 以后重复扫过 FPC 主干、扇出和黑胶边缘。

新增视觉证据：

- `P16` 是最外侧机械触点；其可见铜箔在连接端形成短盲端，没有继续进入下方扇出。相邻的 `P15-P1` 可见线路继续向黑胶/扇出方向延伸。这与照片和 `P16-P13 = OL` 的结果相互印证，`P16 = NC/未布线触点` 继续保持 `STRONG_CONFIRMATION`。
- 在可见 FPC 铜层、连接器边缘和玻璃/ACF 扇出区域，没有看到明确的外部铜桥、烧蚀或断裂。梳状、U 形和回折细线属于玻璃侧/ACF 扇出结构，不能当作连接器引脚之间的桥接。
- `P6`、`P13` 都有可见独立线路，未见其为机械空脚；但视频没有给出足够的连续、同角度、无遮挡画面把每根线从金手指完整追到黑胶下的驱动芯片。

证据边界：

- 视频加强了“16 个机械触点、15 条有效可见线路、P16 NC、无明显外部损伤”的结构结论；没有新增可确认的功能针名。
- 仍不能仅凭视频确定 `P6/P13` 谁是 `VSS/VDD/IREF`，也不能确定 SSD1306、SSD1312、SH1106 等控制器或 SPI/I2C 接口。黑胶下的内部互连不可见。
- 因为画面存在移动、旋转、曝光变化和多次回扫，不能把不同时间段的局部线条拼成逐针连续走线；不得据此恢复 3.3 V、U8g2、SPI/I2C 或主动 GPIO 测试。

本轮结论标签：`CONFIRMED_STATIC`（视频中可见的结构与 P16 盲端）；`UNCONFIRMED`（功能针序、控制器、协议和上电时序）。下一步仍是寻找同源完整模块/正式 pin table；三片样品保持断电和全脚悬空。

## 2026-09-23：公开资料检索结果与同类件线索

对 `100655001P`、`100655001P D7/N4`、`30033`、`A0201001` 及其组合进行了只读公开检索。Google 和 1688 没有返回可信的 OLED 产品、控制器或正式针脚表；`A0201001` 的主要命中是汽车滤芯等无关料号，`30033` 的主要命中也是无关产品。这个负结果不能证明料号不存在，只说明它们目前不能作为公开检索键闭环。

找到的同类件线索：

- [Alibaba 1.09 inch OLED 16-pin FPC 检索](https://www.alibaba.com/trade/search?SearchText=1.09+inch+OLED+16+pin+FPC&has4Tab=true&tab=all) 的结果簇包含 GoldenMorning `E109XI-W-C16-03`（SSD1312、64x128、16P、SPI/I2C）以及 Boyida、XLW、JYC 的相近 16 针变体。
- [GoldenMorning E109XI-W-C16-03 规格书](https://goldenmorninglcd.com/wp-content/uploads/2025/06/E109XI-W-C16-03-V1.pdf) 是可向供应商索取 drawing 的具体型号线索；本轮没有把它的针序套到样片上。公开页面没有把 `100655001P`、`30033` 或 `A0201001` 与该型号关联起来。
- [Adafruit 5228 参考裸屏](https://www.adafruit.com/product/5228) 明确是 `1.3 inch / 128x64 / SH1106G / 16-pin 0.5 mm FPC`，其[公开数据表](https://cdn-shop.adafruit.com/product-files/5228/5223-ds.pdf)和页面索引列出了常见公版节点（`C2P/C2N/C1P/C1N/VBAT/IREF/VSS/VCOMH/VDD/VCC/CS#/D-C#/SCLK/SDIN/RES#`）。这只能证明“同类 16 针 COG OLED 的公开参考拓扑存在”，不能证明本屏使用 SH1106G 或同一针序。

判定和下一步：

- 上述同类件统一标记为 `REFERENCE_ONLY` / `UNCONFIRMED`，不改变本项目当前针序结论；尤其不能因为“16 针、0.5 mm、128x64”相似就给 P6/P13/P15 供电或运行 U8g2。
- 若联系供应商，优先索取 `E109XI-W-C16-03` 或 Adafruit 5228 的裸屏照片、FPC 外形尺寸、触点方向、完整 pin table 和 controller part number，并逐项与样片的 `30033/A0201001`、P16 盲端、15 条有效主干进行比对。
- 只有机械外形、触点方向、FPC 走线、控制器和料号同时闭合，才可把公开针表升级为本屏候选；在此之前三片样品继续断电、P1-P16 全部悬空。

## 2026-09-24：Adafruit 5228 机械外形排除

重新核对了 [Adafruit 5228 产品页](https://www.adafruit.com/product/5228) 和产品图（[正面/顶视](https://cdn-shop.adafruit.com/970x728/5228-02.jpg)、[背面](https://cdn-shop.adafruit.com/970x728/5228-01.jpg)）。页面明确给出：`1.3 inch / 128x64 / SH1106G`，显示器本体为裸模块，使用 `16-pin 0.5 mm pitch FPC flip-lock connector`。这些参数说明它属于同一类 16 针 COG OLED 参考件，但不等于与样片同源。

与本项目样片逐项比较：

| 特征 | Adafruit 5228 公开图 | 本项目样片 |
|---|---|---|
| 玻璃外形 | 横向、较宽的 1.3 英寸显示区域 | 纵向窄长玻璃 |
| FPC 位置/形态 | 居中附近的窄短排线，服务于模块化安装 | 顶部宽排线，长主干进入黑胶/ACF 区 |
| 排线标识 | 产品图可见的公版/模块标识 | `30033`、`A0201001`，另有 `16` 方向标记 |
| 连接端结构 | 作为配套模块的 16P 0.5 mm 接口 | 16 个机械触点中 P16 可见铜箔终止，P1-P15 进入驱动区 |

因此，Adafruit 5228 的机械来源可以标记为 `EXCLUDED_AS_SAME_SOURCE`：它不能解释本样片的纵向玻璃、宽 FPC、丝印或 P16 盲端。它仍保留为 `REFERENCE_ONLY`，仅用于说明常见 16P/0.5 mm OLED 的控制器和外围拓扑可能长什么样；不得把其 SH1106G 针表、电压或初始化代码套到本屏。

本次排除不能推出本屏不是 SH1106G，也不能在 E109XI、Boyida、XLW、JYC 等同类 16P 型号之间做正向识别。下一项有效信息仍是量化样片外形：玻璃宽×高、有效显示区宽×高、FPC 最宽处、触点总宽/间距，以及黑胶边缘到触点端的距离；这些数据应与公开 drawing 同时比对。三片样品继续断电、P1-P16 全部悬空。

## 2026-09-24：GoldenMorning E109XI 图纸复核与精确型号排除

通过 [GoldenMorning 产品页](https://goldenmorninglcd.com/oled-display/1.09-inch-oled-64x128-ssd1312-e109xi-w-c16-03/)及其[一页 PDF 图纸](https://goldenmorninglcd.com/wp-content/uploads/2025/06/E109XI-W-C16-03-V1.pdf)复核了 `E109XI-W-C16-03`。图纸明确写出：`1.09 inch`、`64x128`、`SSD1312`、`SPI/IIC`；面板外形约 `14.0 x 31.96 mm`，偏振片约 `13.4 x 28.3 mm`，有效区约 `10.86 x 25.58 mm`。FPC 为 `16` 针、`0.5 mm` 节距，触点跨度 `7.5 mm`，尾部宽度约 `8.5 mm`。这些尺寸和纵向外形与样片明显比 Adafruit 5228 更接近，因此它是有价值的机械参考家族。

该图纸的完整针表为：

```text
P1  C2P        P5  VDD2(VBAT)  P9  VSS       P13 RES
P2  C2N        P6  VPP(VCC)    P10 IM1      P14 VDD1
P3  C1P        P7  VCOMH       P11 CS       P15 SCL
P4  C1N        P8  IREF        P12 DC       P16 SDA
```

关键冲突在 `P16`：E109XI 图纸把 P16 作为有效 `SDA`，其 FPC 图示为连续的 16 条连接；本样片的照片、WMV 逐段复核和断电 `P16-P13 = OL` 共同显示 P16 铜箔在连接端形成圆头短盲端，P1-P15 才继续进入黑胶/ACF 区。因此：

- `E109XI-W-C16-03`：`MECHANICALLY_SIMILAR_REFERENCE`；
- `E109XI-W-C16-03` 作为本样片的精确型号：`EXCLUDED_BY_P16_CONFLICT`；
- 其针表、电压、外围电容和初始化代码：不得套用到本样片。

这次复核把候选范围收窄为“同一类纵向 1.09 英寸/16P/0.5 mm COG OLED 的变体”，但还没有找到能同时解释 `30033`、`A0201001`、P16 未布线和样片完整走线的公开料号。样片的玻璃宽×高、有效显示区、FPC 最宽处和黑胶到触点端距离仍需用游标卡尺实测，再与其他供应商 drawing 逐项比对；在闭环前三片样品继续断电、P1-P16 全部悬空。

### GoldenMorning 图纸与样片的可比项目

后续拿游标卡尺测量时，优先按同一基准记录以下项目（不要只量黑色显示区）：

```text
样片玻璃/面板外框：宽 × 高（mm）
样片偏振片：宽 × 高（mm）
有效显示区：宽 × 高（mm）
FPC 最宽处：mm
触点总跨度：mm（理论值为 15 个间距）
黑胶/ACF 最近边到触点端：mm
```

如果样片接近 `14.0 x 31.96 mm` 面板、`13.4 x 28.3 mm` 偏振片、`10.86 x 25.58 mm` 有效区和约 `8.5 mm` FPC 尾宽，只能说明它属于同一机械尺寸族；只有同时满足料号、P16 走线和正式针表，才可进一步谈同源。当前 P16 盲端与 E109XI 的 P16=SDA 已是明确冲突，不能用尺寸相近抵消。

### ZJY 1.09 英寸近邻件排除

公开的[中景园 `ZJY109-6428TSWEG04` 页面](http://www.zjydisplay.com/product/77.html)给出 `64x128`、约 `10.86 x 25.58 mm` 显示区和约 `14 x 31.96 mm` 面板，外形尺寸与 E109XI 家族接近；但其规格明确为 `15 pin / 0.65 mm` 焊接 FPC。样片已经由照片、游标卡尺记录和转接板规格确认是 `16` 个触点、`0.5 mm` 间距，因此 ZJY 这个公开型号可标记为 `EXCLUDED_BY_CONNECTOR`。它只能保留为尺寸参考，不能提供样片针序。

### 卖家照片中的尺寸与料号线索

项目附件中的卖家照片（例如 `pic/O1CN014NXK4h2BwdIGmF1Ys_!!2200671998403.heic`）在画面上标注显示区约 `13 mm x 25 mm`、FPC 接触区约 `8.5 mm`；包装照片中还能看到多片标签带有 `100655001P` 基础串及不同后缀。这与样片的纵向窄长玻璃、约 `8.5 mm` 尾部和 `0.5 mm` 触点几何相容，也说明 `100655001P` 更像供应链/批次家族键，而不是公开控制器型号。

这组数值属于卖家图片上的示意标注，不是经过校准的样片实测；不能用它替代游标卡尺，也不能由 `100655001P` 直接推出 SSD1312、SH1106G 或任何针名。后续记录应把卖家值和实测值分栏保存。
